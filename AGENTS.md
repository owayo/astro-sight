# astro-sight

AI エージェント向け AST 情報生成 CLI (Rust)

## Architecture

- **AppService 層** — CLI / Session / MCP の統一コアロジック（`src/service.rs`）
- **tree-sitter** ベースの構文解析エンジン（16言語対応）
- **動的 import の依存抽出** — JavaScript / TypeScript / TSX の通常の import 文と `require()` に加え、`import("./module")` と置換を含まない `` import(`./module`) `` を `imports` で抽出する。`${expr}` を含む template literal は静的に依存先を確定できないため除外し、呼び出し形式は第1引数だけを依存先として扱う
- **Ruby Unicode 識別子** — simple case folding の対象となる `ſ` / `K` などを含むメソッド名を tree-sitter で正しく抽出する
- **手書き lexer** バックエンド（`src/engine/lexer/`: `mod.rs` = 汎用 state machine、`xojo.rs` = Xojo 固有規約）。tree-sitter で OOM する言語向けの fallback として v26.6 で導入。現状 Xojo を lexer-only でサポート（`symbols` / `refs` / `dead-code` のみ動作、`calls` / `imports` / `ast` / `lint` / `sequence` は `UNSUPPORTED_LANGUAGE` を返す）。modifier ループは複数修飾子を順番に消費する（`Public Shared Sub Foo()` のような可視性 + Shared/Static 併記の標準記法を `Shared` 消費後に定義 keyword と突き合わせる、v26.6.110）。同一 keyword の重複は consumed リストで除外して病的入力でも無限ループしない
- **BLAKE3** ベースのファイルキャッシュ（単一ファイル `ast` / `symbols` は内容ハッシュに canonical path と astro-sight バージョンを混ぜ、同一内容の別ファイルや別言語で `path` / `lang` が混線せず、バージョン更新時は解析ロジック/出力スキーマの変更に追従して旧キャッシュが自動失効する＝内容不変でも結果が変わるケースで stale を防ぐ。書き込みは同一ディレクトリ temp + rename の atomic 方式で、kill / 電源断でも truncated JSON が残らない。読み出しは末尾 `}` の軽量検証付きで、不正エントリは miss 扱い + 自己削除。`cmd_ast` / `cmd_symbols` は put 前に service が実際に解析した内容の hash (`response.hash`) と cache key の元になった read 内容の hash を照合し、2 回の read の間にファイルが更新された場合は put しない＝hash(旧内容)→output(新内容) の TOCTOU キャッシュ汚染を防ぐ）
- **clap derive** による CLI 引数パーサー
- **NDJSON** ストリーミングセッション対応
- **スマートコンテキスト**（diff→影響分析、関数シグネチャ変更は識別子境界で照合して prefix 名の別関数を誤検出しない、単一/バッチ参照検索は fold/reduce でピーク RSS を抑制、バッチ参照検索 O(N+S)、`find_references_batch` はディレクトリ走査・AC 走査・parse をすべて名前数に依らずファイル毎 1 回に集約する。AC は原則 1 個 — aho-corasick 1.1 の trie は実測 5 万パターン ≈ 8MB / 20 万 ≈ 30MB とパターン数にほぼ線形で、旧設計の「chunk 分割 (既定 64) で trie メモリを上限化」は根拠を失っており、chunk 毎に全ファイルを再読込・再パースするコストだけが残っていた (実測: 全ファイルヒットの名前構成で chunk 2 個 = wall 2 倍)。`refs_ac_split_size()` (既定 100,000、`ASTRO_SIGHT_REFS_BATCH_CHUNK` で上書き) を超える病的な入力だけ AC を分割するが、その場合もファイル走査と parse は 1 回のまま。Angular テンプレート参照は `AngularBatchContext::prepare(dir, glob)` で `is_angular_project` 判定 + `collect_component_templates` を 1 度だけ実行して再利用する（非 Angular リポでは `None` で完全 skip））
- **プロセス内再利用による解析高速化 (v26.8)** — (1) tree-sitter `Query` を `(LangId, クエリ文字列)` 単位で `LazyLock` キャッシュ (`engine/query_cache.rs`)。`Query::new` のパターン解析は TypeScript 級の文法でファイル parse 本体の数倍重く、旧実装は symbols / calls / imports / lint / member liveness がファイル毎に再コンパイルしていた (実測: `symbols --dir` 3000 ファイルの CPU の 7 割超がクエリ再コンパイル。キャッシュ化で 2.5s → 0.5s)。custom query の無制限流入に備え上限 512 件、超過後は都度コンパイルへフォールバック。(2) `Parser` は thread-local に言語別再利用 (`PARSER_POOL`)。timeout で halt した Parser は resume 状態を持つため pool へ戻す前に必ず `reset()`。(3) dead-code の member liveness (JS/TS/PHP) はループ反転 — 旧実装は duplicate set 毎に全ファイルを main thread で再 read + 再 parse していた (set 数 × ファイル数)。ファイル毎 1 回 parse で全 set を解析し rayon 並列化 (`SetAccum` の可換マージで結果は決定的。実測: dead-code 3000 ファイル 103s → 7s)。(4) `collect_files` はファイル内容を読む判定 (generated マーカー / shebang) を walk 後の並列ステージへ後送し、言語判定を先に通して parse 対象外ファイルの open 自体を省く (出力集合・順序は不変)。(5) refs の AST 走査は単一 TreeCursor の反復 pre-order (`walk_refs`、旧実装はノード毎に `node.walk()` で TreeCursor を C malloc)。(6) 参照検索の rayon worker 既定を available_parallelism に引き上げ (`ASTRO_SIGHT_BATCH_WORKERS` で低メモリ環境向けに下げられる)。(7) PHP の単一名事前フィルタは ASCII CI searcher を検索開始時に 1 回構築して共有 (旧実装は PHP ファイル毎に source 全文の lowercase コピーを確保)。いずれも出力は不変で、変更前バイナリとの全コマンド出力 byte 一致を確認済み
- **ローカルスコープの影響抑制** — 関数内で宣言されたローカルシンボルはファイル間影響の起点から除外する。TypeScript/JavaScript、Rust、Python、Go、Java、Kotlin に対応し、Kotlin 固有の `function_body` を認識してネスト関数とトップレベル関数を区別する
- **型注釈位置は Reference (全言語)** — `is_definition_context` (`refs/definition/mod.rs`) は `LangId` を全列挙する `match` で、戻り値型・基底クラス位置の識別子を def と誤判定しない。判定方式は文法ごとに 3 通り: (a) **`name` フィールド一致** — JS/TS/TSX・Go・Java・C# (`is_name_field_definition_context`)、(b) **`name` フィールドのうち identifier 系** — Swift (`function_declaration` は関数名と戻り値型の**双方**が `name:` で現れるため `child_by_field_name` の「最初の一致」順序依存に頼らない)、(c) **最初の identifier 系の子** — Kotlin (宣言ノードが `name` フィールドを持たない) と Go の `package_clause` (同上)。未移行の Rust / Bash / Xojo は `is_ancestor_kind_definition_context` (旧 parent/grandparent 走査) を明示的に呼ぶ＝catch-all を置かないので言語追加時にコンパイルエラーで気付ける。修正前は Go/Java/Kotlin/Swift/C# で「基底クラス」「戻り値型でしか使われない型」が dead-code に出て、`api.add` の `ri` も過小になっていた
- **Rust closure 束縛のシャドーイング** — `refs` は名前一致だけで数えるため、`|(_, tail)| tail` のようなローカル束縛が同名関数への参照として計上され、本番参照ゼロの関数が live に見えていた (dead-code の fail-open)。`refs/definition/rust.rs::is_rust_closure_bound_identifier` が walker の共通 identifier 経路で除外する (single / batch / visitor / count-only が一致)。スコープは closure に限定する — 「パラメータが body 全体をシャドーイングする」ことが構文だけで確定するため (`let` shadowing / match arm / `for` は宣言順と scope boundary の追跡が要るので別 Issue)。**逆向きの事故 (live を dead にする) を避ける制約が本質**: (1) 修飾参照 (`crate::tail()` / `Type::tail()`)・メソッド名 (`field_identifier`)・型位置・マクロ名 (`tail!()`) は値束縛にシャドーイングされないため `is_rust_shadowable_value_identifier` で除外対象から外す (早期 return で祖先走査のコストも省く)、(2) パターン中の bare identifier は必ずしも束縛ではない — 単位構造体 (`|Unit|`) や定数 (`|MAX|`) との照合も同じノードなので、`rust_pattern_name_is_binding_style` が命名規約 (先頭の `_` を読み飛ばして最初の実文字が大文字でない) で保守側に倒す。`_Unit` を先頭 1 文字だけで判定すると `_` が小文字扱いになり参照を消す、(3) `tuple_struct_pattern` / `struct_pattern` の `type:` と `field_pattern` の `name:` は型名・フィールド名なので辿らない、(4) 未知のパターン種別は辿らず false (取りこぼし = 従来どおりの過大計上に戻るだけ)
- **Rust の実効可視性 (メソッド)** — `pub(crate) struct` の inherent impl 内 `pub fn` はクレート外から到達できないため外部公開 API ではない (実効可視性 = min(宣言, 所有型, モジュール))。`ExportSurfaceContext::rust_owner_type_is_crate_internal` が qualname の owner 名で同一ファイル内の型宣言 (Struct / Enum / Class に限定) を引き、`symbols::is_rust_declaration_unrestricted_pub` で可視性を判定する。**可視性は宣言行のテキストではなく AST の `visibility_modifier` で見る** — Rust はトークン間の改行・コメントが有効で、`pub\nstruct Holder;` の宣言行は `"pub"`、`pub /* c */ struct Holder;` は `"pub /*"` になり、行テキスト判定では crate 内部と誤判定して公開 API の変更を取りこぼす (fail-closed 要件と逆方向)。`impl` ブロック自体も同名 container として収集される (kind = Type) が `impl` 行に `pub` は書けないため kind で除外する (混ぜると全 inherent impl メソッドが内部扱いに落ちる)。owner が別ファイル / 同名の型宣言が複数 / 判定不能は fail-closed で公開扱い維持。モジュール階層と `pub use` 経路の到達性は `rust_public.rs` が別途見る
- **AST 応答の巨大行抑制**（`ast` の `text` / `snippet` は 256 文字上限で切り詰め、巨大行で応答が肥大化しない）
- **`.h` ヘッダの C/C++ 自動判定** — `.h` は既定では C として扱うが、C++ 専用構文マーカーがあり、C++ parser の方が C parser より parse error が少ない場合だけ C++ に切り替える。通常の C ヘッダを不用意に C++ 扱いせず、`class Foo { public: ... }` や `struct X : Base<X> {}` のような C++ ヘッダの `symbols` / `review` / `dead-code` 取りこぼしを抑える
- **MCP サーバーモード**（rmcp 3.1 による stdio JSON-RPC 2.0、ワークスペースサンドボックス、11 ツール。rmcp 2.x 以降では `Content` が `ContentBlock` enum に統合されている）
- **デフォルト compact JSON** 出力（`--pretty` で整形出力）
- **出力フォーマット (JSON / TOON / auto)** — `--format json|toon|auto` (グローバル) と `config.toml` の `format` で切り替える。優先順位は **CLI `--format` > `config.toml` > `json` (既定)**。解決結果は `OutputOptions` (`src/output/mod.rs`) にまとめ、commands 層だけへ渡す (`AppService` / `models` には持ち込まない = 解析結果と表現の分離)。TOON エンコーダは encoder-only の自前実装 (`src/output/toon/`) — crates.io の `toon-format` は通常依存で `serde_json` の `preserve_order` feature を有効化し、cargo の feature unification でワークスペース全体の `serde_json::Map` が `BTreeMap` → `IndexMap` に変わる = **既存 JSON 出力 (hook 等) のキー順が変わる**ため採らない (加えて最新リリースが spec v3 世代、default feature が ratatui / syntect / arboard を引く)。判定には `cargo tree --edges normal,features -i serde_json` を使う。引数なしの feature tree には tree-sitter のビルド依存が有効化した `preserve_order` も現れるが、Cargo resolver が実行時依存と分離するため JSON 出力には影響しない。`--pretty` は JSON 専用で TOON では無視する (TOON は元からインデント構造で compact / pretty の対立が無い。NDJSON バッチで従来から `--pretty` を無視しているのと同じ扱い)。CLI の TOON は canonical encoder の要件どおり、単一・バッチ・`auto` 選択時のすべてで末尾改行を付けない。JSON / NDJSON の改行終端は既存契約として維持する
- **`--format auto`** — その出力について compact JSON と TOON を両方エンコードし、**推定トークン数が小さい方**を採る。指標は `output::size_metric` = `文字数 (Unicode スカラー値) + 3 × 改行数`。素の文字数にしないのは、BPE では改行とインデントが 1 行あたり約 1 トークンを消費し、行数の多い TOON を過大評価するため — 実測で `{"a":1,"b":2,"c":3,"d":4}` (25 文字 / 17 tokens) と `a: 1` `b: 2` `c: 3` `d: 4` の 4 行 (19 文字 / 19 tokens) は文字数と実トークン数で勝者が逆転する (o200k_base / cl100k_base 共通)。係数 3 は 1〜4 の平坦域の中央値で、astro-sight 出力 1,127 サンプルの実測では **1 回の呼び出しあたりの最悪損失が 素の文字数の +28 tokens (15%) から +1〜2 tokens** へ下がり、全体では真の最小との差が 0.0001〜0.0008% に収まる (1,127 件中 564 件で JSON、563 件で TOON が選ばれた = `auto` は「常に TOON」ではない)。実トークナイザを積まないのは (1) BPE テーブルで数 MB 増える (2) 消費側モデル / tokenizer 版で結果が変わり再現性が壊れる (3) 推定値で十分近い、の 3 点。同点は JSON (既定フォーマットで消費側の互換性が高い)。常に両候補以下になるので「auto にしたら長くなる」ことは構造的に起きず、選択は入力内容だけで決まるため決定的。`--pretty` は「選ばれた JSON の描画方法」だけを決める (比較は常に compact JSON と TOON で行うので、TOON が勝てば `--pretty` は効かない)。**空 object は TOON では空ドキュメント = 無出力になる (§8) ため auto は JSON を選ぶ** — 「結果が空」と「何も出力されなかった」を利用者が区別できなくなるのを避けるため (明示的な `--format toon` は仕様どおり空ドキュメントを出す)。プロトコル面 (session / hook / エラー) では `auto` をエラーにしない — 「TOON で出せ」という満たせない要求ではなく、JSON を選ぶことも auto の正当な結果のため。キャッシュは対象外 (内容で出力形式が変わるうえ、truncation 検査が末尾 `}` 前提)
- **auto のバッチ判定** — `--paths` / `--paths-file` / `--dir` は解析結果を全件バッファしない設計のため、全レコードを見てから勝者を決められない。**最初の window (既定でワーカー数 × 8 件) を両形式で描画し、その実測値 (TOON はヘッダ 1 行ぶんを加算) で勝者を決めて以降の window に適用する** (`BatchRendered::Both` → `One`)。二重エンコードのコストは先頭 window ぶんだけで、解析自体はどの経路でもパス 1 回きり。決定は 1 度だけなので出力が途中で混ざることはない。`refs --names` は元々全件を保持しているので近似せず、NDJSON 全体と TOON ドキュメントを実際に組み立てて比べる
- **出力面ごとのフォーマット適用範囲** — データ面 (単一ドキュメント / バッチ / MCP の tool result text) は `--format` に従う。**プロトコル面は常に JSON 固定**: `session` (1 行 = 1 リクエスト/レスポンスの NDJSON)、`review --hook` / `impact --hook` (Claude Code の Stop hook が消費する JSON 契約)、トップレベルのエラー行 (既存スクリプトが parse する機械可読契約。format 解決自体の失敗も報告するため bootstrap 問題を避ける)。**CLI で明示された `--format toon` はエラー、`config.toml` 由来の既定値は黙って JSON へ倒す** (`OutputOptions::ensure_json_protocol`) — そうしないと `format = "toon"` を設定しただけで session と Stop hook が全滅し、設定として使い物にならない
- **TOON バッチ出力** — `--paths` / `--paths-file` / `--dir` は JSON では従来どおり NDJSON、TOON では **ルート配列 1 個のドキュメント** (`[N]:` + list item)。要素数 `N` は入力パス数と 1:1 で先に分かるため、解析結果を全件バッファせずヘッダを先出しでき、ピーク RSS を入力件数から独立させる既存要件を保てる。外側配列を tabular form にするには全要素を見る必要があり streaming と両立しないので list form を使う (削減量の大半は内側配列の tabular 化から来るので実害は小さい)。解析失敗レコードも **必ず 1 要素として出す** — ヘッダの `[N]` と item 数が食い違うと strict decoder が落ちるため。`refs --names` は元々全件を `Vec` で保持しているので、TOON では canonical な 1 ドキュメントとして出す。`context` の TOON は pretty JSON と同じ集約経路を通る (ルート配列の `[N]` が解析完了まで確定しないため streaming 不可)
- **TOON の nullable 列正規化** — compact DTO は `skip_serializing_if = "Option::is_none"` を多用するため、`symbols` の 1 要素は関数なら `{name,kind,ln,cx}`、定数なら `{name,kind,ln}` になる。TOON の tabular form は配列内の全 object が同じキー集合を持つことを要求するので、素直にエンコードすると list form へ落ちて **JSON より冗長になる** (実測 `symbols --dir` +33%)。そのため `src/output/nullable.rs` が「キーが欠けているだけ」の配列に `null` を補って tabular を成立させる。適用は **厳密エンコードより短いときだけ** (同点は厳密側) なので TOON が JSON より長くなることは構造的に起きない (実測は全コマンドで -17% 〜 -60%)。補完対象は全要素が非空 object かつ全値がプリミティブの配列に限り、列順はキーの初出順で固定する (決定的)。**JSON 表現との構造的 round-trip は保証しない** (省略キーが `null` として現れる。DTO の `Option<T>` としての意味は保存される)。この判断は astro-sight が自分の DTO について行うもので、汎用エンコーダ (`src/output/toon/`) 側には入れない — 任意の JSON に対して欠損キーを null 補完すると「明示的な null」と「未設定」を区別できなくなるため
- **バッチ処理**（`--paths` / `--paths-file` で複数ファイル NDJSON 出力。専用 rayon pool の既定ワーカー数は `min(available_parallelism, 4)` とし、巨大ファイル解析後に thread-local Parser が保持する作業領域の総量を抑える。`ASTRO_SIGHT_BATCH_WORKERS` の正の整数指定で利用可能 CPU 数まで上書き可能。入力順を保つ rayon chunk はワーカー数の 8 倍を上限とし、未排出結果のピーク RSS を入力件数から独立させる。stdout 書き込み失敗は chunk 境界で検出して残りの解析を停止）
- **API 互換判定の member 参照バッチ化** — TS/JS object から削除された複数 member の利用有無は、削除キーごとに全ファイルを再走査せず、単一 Aho-Corasick matcher と `HashSet` で候補を絞ってファイルごと 1 回だけ read / parse / AST walk する。ファイル収集・matcher 構築・read・parse の失敗は「参照あり」に倒し、未参照削除へ誤降格しない fail-closed を維持する
- **JSON エラー出力**（`{"error":{"code":"...","message":"..."}}` を stdout に出力。stdout pipe が先に閉じた場合は broken pipe panic を出さず exit 0 で終了）
- **入力検証の強化**（`refs` の空 `name/names` を拒否、`--paths` / `--paths-file` の空リストを拒否、`--paths-file` は 100MB 上限付きで読み込み、`cochange` の `min_confidence` / smoothing prior は有限範囲に制限、`session` の空文字・非 UTF-8・不正パスの `ASTRO_SIGHT_WORKSPACE` を拒否、`--dir` にはディレクトリのみ許可、streaming `context` は JSON prefix 出力前に入力を検証）
- **セキュリティ** — パス境界チェック（Session/MCP のワークスペースサンドボックス、相対パスはワークスペースルート基準、非 UTF-8 パスは canonicalize 後に fail-closed で拒否、`review --diff` 経由の `detect_api_changes` / `filter_diff_files_for_dead_code` も diff の `new_path` / `old_path` を `is_safe_diff_path` でトラバーサル検証。`should_skip_diff_file` は `is_safe_diff_path` の文字列 check に加えて、新ファイルを `canonicalize().starts_with(canonical_dir)` で fail-closed 判定し、`evil.rs -> /etc/passwd` のような symlink で workspace 外を指す追加ファイルからの識別子リークを阻止する）、ファイル/入力サイズ 100MB 上限（`session` / `context` / `impact` の生入力、`--paths-file` を含む、`parser::read_file` は metadata 取得後に拡大する TOCTOU 経路も `take()` + 読み込み後再検証で阻止）。Angular テンプレート（2MB）と Android XML（1MB）の補助スキャンも `bounded_read` で上限 + 1 byte だけ読み、metadata 後の差し替え・拡大を検出する。`git diff` / `git show` / `git blame` に渡す `--base` 等 revision は `-` プレフィクス / NUL / 空文字を拒否（オプション誤認識防止、`cochange --blame` の `--paths` / `--paths-file` 経由でも検証）。`git show <rev>:<path>` は `git_input.rs::git_show_blob` が唯一の実行点で、revision と path の `validate_git_revision` を必ず通す＝呼び出し側が検証を書き忘れる余地を構造的に無くす（旧実装は 15 箇所が個別に `Command::new("git")` を書き、検証の有無と失敗時ポリシーを各々で決めていた。検証失敗 / git 実行失敗 / 当該 rev にファイル無しはすべて `None` に畳む＝全呼び出し側が同じ保守側分岐に倒れるため区別不要）。`cochange` の `source_files` も `service.rs::is_contained_relative` で `--dir` 配下の相対パスに閉じる（`Path::components()` で `..` / POSIX 絶対パス / Windows のドライブ修飾 `C:foo` を一括判定する＝文字列 prefix 判定では drive-relative 形式を取りこぼす）。`source_files` は git の pathspec として `--` の後ろに渡るためオプション注入は起きないが、`current_dir(dir)` 基準で解決されるため `../` を含むと **サンドボックス外**にある同一リポジトリ内ファイルを blame でき、`commits_analyzed` の 0/非 0 が存在オラクルに、`entries[].file_b` がパス列挙になっていた（`dir` だけを検証し `source_files` は素通しだった＝他のパス系 API が全て `validate_path` / `validate_dir` を通しているのに対する検証系列の漏れ）。blame は base リビジョン側の履歴を辿るため working tree に実体が無いファイルも正当な入力であり、canonicalize による実在確認はせず字句レベルで判定する
- **非 ASCII ファイル名対応** — ファイル名を stdout から解析する git 呼び出し（`run_git_diff` / blame source の `diff --name-only` / cochange の `diff-tree --name-only` / `changed_new_lines_for_file`）は `-c core.quotepath=off` を付け、日本語等のファイル名が 8 進クォート (`"\346..."`) されて diff ヘッダ照合が崩れるのを防ぐ。防御として `parse_unified_diff` は認識できない `--- ` / `+++ ` ヘッダ行で state を flush/リセットし、quotepath 形式の外部 diff でも hunk が直前ファイルへ誤帰属しない（該当ファイルは解析対象外に落ちる fail-safe）
- **トークン最適化** — version フィールド省略（doctor/MCP のみ）、compact キー短縮（`lang`/`ln`/`col`/`ctx`/`refs`/`src`/`def`/`ref`/`fn`/`cx` 等）、calls を caller グルーピング、CompactAstEdge フラット化、refs/context で相対パス出力、symbols デフォルト compact 出力（`--doc` で docstring 付加、`--full` で旧来の完全出力、`-q/--query` でカスタム tree-sitter クエリが built-in を置換 — capture 名は `function.name` / `class.name` 等の既存語彙に限定し、不正クエリ・未知 capture・lexer-only 言語は `INVALID_REQUEST`。cache key には query hash を混ぜ、default query の key は不変。`--path` / `--paths` / `--dir` / session の `query` フィールドで有効）
- **dead-code 規約除外 / member liveness** — PHPUnit 規約 (`*Test` / `*TestCase` クラス、`testXxx` / `setUp` / `tearDown` 等) と Python unittest/pytest 規約 (`unittest.TestCase` 派生クラス + 同一ファイル内の間接継承を fixed-point で解決、`test_*` / `setUp` / `tearDown` / `setUpClass` / `tearDownClass` / `addCleanup` / `addClassCleanup` メソッド、`test_*.py` / `*_test.py` のトップレベル `test_*` 関数、`conftest.py` 内の関数) を dead-code から除外（テストランナーが動的 discover するため）。Angular `@Component` / `@Directive` 装飾クラスのライフサイクルフック (`ngOnInit` / `ngOnDestroy` / `ngOnChanges` / `ngDoCheck` / `ngAfterContentInit` / `ngAfterContentChecked` / `ngAfterViewInit` / `ngAfterViewChecked`) も Angular ランタイムが change detection サイクルで自動呼び出しする暗黙エントリポイントのため dead-code から除外。Xojo (lexer-only) では `#tag Event` 〜 `#tag EndEvent` 配下のイベントハンドラ (`Sub` / `Function`、Xojo ランタイムがイベント駆動で呼ぶ) と、`Inherits TestGroup` を直接/同一ファイル内間接 (fixed-point) に継承するクラスの引数なし `*Test` / `Setup` / `TearDown` メソッド (XojoUnit が Introspection で列挙・実行) を runtime entrypoint として dead-code から除外 (`lexer/xojo.rs::runtime_entrypoint_lines`、定義行 (range) で照合し bare name 除外による汎用名 (`Action` / `KeyDown`) の巻き込みを回避、dead-code 経路のみで API 差分では公開 API 面として残す)。TS/JS と PHP の duplicate な class member は owner を安全に一意推定できる場合だけ従来の bare name 重複スキップを緩和する。TS/JS は **receiver-aware 帰属**: `Owner.member` (static access — **import または同ファイル class 定義に限定**、単なる owner 名一致では確定しない) / `new Owner().member` / owner 型 annotation・`new Owner()` 初期化子付き変数 / `Owner[]` 配列変数の `xs[i].member` / enclosing class が owner の `this.member` だけを確定票とし、factory・DI・関数戻り値など receiver を静的に辿れない access が 1 件でもあれば set 全体を Ambiguous に倒す (import 有無ベースの帰属は廃止)。同名のローカル binding があれば shadow とみなし変数として判定する (`const Alpha = getBeta(); Alpha.fmt()` を class access と誤認しない)。binding 収集 (`collect_var_bindings`) は variable_declarator / typed パラメータの単純 identifier だけ owner 解決を試み、それ以外の binding 導入構文 — for-of・for-in の**宣言付き** loop 変数 (`for (const X of xs)` の left は lexical_declaration に包まれない bare pattern。kind の無い `for (x of xs)` は代入なので対象外)・catch パラメータ・bare arrow / JS formal_parameters 直下のパラメータ・destructuring (assignment_pattern は left のみ辿り default 式の参照を binding 扱いしない)・関数宣言 / named function・class **式**の名前 — は `register_pattern_bindings` で `Unresolvable` 登録する (収集漏れ = shadow 見逃し = import owner への誤帰属 (dead 誤検出の fail-open) になるため。class_declaration 名は `collect_local_class_definitions` が正票の源として登録するので対象外)。冗長括弧付き receiver (`(new Owner()).member`) は剥がして実 receiver で判定する。`import * as ns` 経由のアクセス (`ns.Alpha.fmt()`) は receiver が member_expression になり通常分類で不解決に落ちるため、namespace import の存在だけでファイル全体を Ambiguous にはしない。PHP は `Owner::method()` と同一クラス内 `self::method()` を確定参照として数え、`$obj->method()` や callable 文字列など owner 不明の参照があれば ambiguous として旧スキップへ戻す。`__construct` の duplicate set は `new Foo()` (object_creation_expression) をクラス名票として数える (`new $var()` / `new static()` / `new parent()` は Ambiguous、bare 名 prefilter は `new` を含むファイルも parse 対象に残す)。`use X\Y as Z;` の alias (grouped use 含む) はファイル単位で収集して `Z::m()` / `new Z()` を実クラスへ解決する (単一 namespace + 競合なしで一意解決できた場合のみ。multi-namespace や競合 alias では alias 名への参照だけ Ambiguous に倒し silent Ignore の dead 誤検出を防ぐ)。`static::` は遅延静的束縛でサブクラス override へディスパッチされ得るため `parent::` と同様 Ambiguous に倒す。trait `use` の合成先 (class / trait / enum、PHP 8.1+) を graph 化して `UseSite::method()` を一意到達の trait method へ解決するが、合成先が同名の**具象**メソッドを自己宣言している場合は PHP の解決順（自クラス > trait）により trait 側へ辿らない（abstract 宣言は trait 実装で満たされるため通常どおり辿る）。member liveness / trait use 収集の走査集合には hidden ディレクトリ配下の diff 候補ファイルも `refs::merge_extra_files` で合流させ、count 経路と一致させる。Angular member decorator（`@HostListener` 等）とメソッドの対応付けは間に挟まったコメントを読み飛ばす。`--dead-scope touched-symbols` は qualname (`Container.method`) の宣言行を bare name fallback で解決し、class member にもフィルタが効く。**Python の字句ローカル定義 (関数 / lambda の内側で宣言された関数・クラス) は公開 API 面から除外する** (`symbols/exported.rs::is_python_lexically_local`)。dead-code は「公開シンボルの未参照検出」だが Python の export 判定は名前の `_` 規約と `__all__` しか見ておらず字句スコープを見ていなかったため、クロージャやデコレータ登録されるハンドラ (`@registry.handler(...)` を付けたネスト async 関数など) が公開候補に漏れ、「直接の呼び出し式が無い」という理由だけで dead と誤報されていた。ネスト定義は外から名前で参照できず、返り値・登録・コールバックとして渡される経路もスコープ解析なしでは追えないため、bare-name の repo-wide 参照検索で生死を判定すること自体が不健全 (真にローカルな dead は検出漏れになるが、それは将来 scope 限定の別解析で扱う)。`symbols` コマンドの列挙は従来どおり。あわせて登録デコレータの末尾名に汎用の `handler` / `tool` / `listener` / `subscriber` / `subscribe` / `consumer` を追加した (`lru_cache` / `wraps` / retry のような「関数を変換するだけ」のデコレータは真の dead を隠すため入れない)
- **C/C++ dead-code liveness 補正** — 本体を持たない前方宣言 / opaque tag (`typedef struct st_mysql MYSQL;` の `st_mysql` 等、外部ライブラリの不透明構造体タグ) は「定義」ではないため dead 候補から除外 (`symbols.rs::is_cpp_forward_declaration`)。body あり `struct` / `class` / `union` / `enum` tag は定義行だけ Definition とし、`struct X *p` / `struct X value` / `sizeof(struct X)` / cast / 関数引数やメンバ宣言内の tag 名は `refs` / dead-code の count-only 経路で Reference として数える。enum は型名が直接使われなくても列挙子のいずれかが参照されていれば live、body あり typedef tag は alias 名経由の参照でも live と判定する (`symbols.rs::collect_cpp_dead_liveness_aliases` で enum→列挙子 / typedef tag→alias を集め、`refs.rs::cpp_typedef_enum_definition_context` で enumerator name / typedef alias 名のみ Definition 分類し宣言行を参照と二重計上しない。typedef の元型・enumerator value 式・配列長式内の識別子は Reference)。すべて `LangId::C | Cpp` で閉じ symbols 公開出力は不変
- **C/C++ メソッド・ポインタ返り関数の抽出** — C++ クラスメソッド (`field_identifier`)、クラス外定義 (`qualified_identifier` の末尾名)、C/C++ のポインタ返り (`pointer_declarator`、`Type *foo()`) / C++ 参照返り (`reference_declarator`、`T& m()`) 関数を `symbols` / `calls` / `dead-code` / `impact` で抽出する (旧クエリは `function_definition > function_declarator` 直結のみで、宣言子が包まれるポインタ返りやメソッドを取りこぼしていた)。クエリは `function_declarator` 配下の名前を直接キャプチャし `cpp_enclosing_function_definition` で本体まで辿るため、本体の無い宣言 (プロトタイプ) は採用しない。C++ メソッドは可視性 (`access_specifier` の public/protected/private、class デフォルト private・struct デフォルト public) を `symbols.rs::is_exported_cpp` で判定し、非公開メソッドの変更を API 差分の偽陽性にしない (自由関数・クラス外定義は保守的に公開扱い。access_specifier は field_declaration_list の直接子なので「parent が field_declaration_list である祖先」= member_anchor から prev_sibling を遡る — template メンバは `field_declaration_list > template_declaration > function_definition` 構造のため method 自身からの遡りでは届かず private 誤判定になる)。C/C++ 関数の doc comment は昇格した function_definition 自身の直前コメントから拾う (name の親 = function_declarator の prev sibling は戻り型で comment に届かない)。Ruby の `class A::B` / `module A::B` は name capture が scope_resolution 内で range が名前に潰れるため class / module ノードへ昇格し、本体全体を range にして配下メソッドの container 帰属を成立させる。`calls` の caller 名は宣言子を末尾識別子まで辿り `foo(args)` のような汚染を防ぐ (`calls.rs::c_function_name_from_declarator`)
- **フレームワーク自動検出** — `--framework` 未指定時でも `<dir>/package.json` の `dependencies` / `devDependencies` に `next` キーがあれば自動で `nextjs` プリセットを適用 (`peerDependencies` / `optionalDependencies` は誤爆しやすいため対象外)。明示指定は常に auto detect に優先する
- **API 差分検出 (api.add / api.rm / api.mod)** — bin-only Rust crate (`src/lib.rs` なし & `Cargo.toml` の `[lib]` セクションなし) の `pub fn` 追加・削除・シグネチャ変更は外部公開 API ではないため API 差分から除外。`api.rm` 経路は base リビジョン側の `Cargo.toml` / `src/lib.rs` を `git show` で取得して判定し、新ツリーで `src/lib.rs` を削除した同時 diff でも旧公開 API の削除は誤抑制しない。C/C++ では tree-sitter が誤パースする実関数 body 内ネスト `function_definition` (`BOOST_FOREACH(...){}` 等のマクロ呼び出し) を exported から除外し (`symbols.rs::is_cpp_nested_function`)、同名シンボルが旧/新いずれかに複数ある場合 (overload / 誤パース) は別物同士の突き合わせを避けるため曖昧として api.mod から除外する。TS/JS では named export clause で公開が継続しているシンボル — `export { foo } from "./b"` (forwarding) に加え、`import type { X } from "...";` + `export type { X };` の **from 句なし re-export** も — は利用者から見た export 面 (import path から取れる名前) が維持されるため `api.rm` から除外する (`symbols.rs::collect_js_ts_named_export_surface_names` が from 句の有無を問わず export clause の公開名を抽出 (alias 対応)。ローカル定義付き `export { X }` の X も集合に入るが、定義が exported symbols として別途現れ api.rm 候補に上がらないため無害。`has_named_export` は from 句付き export をローカル export 判定から除外し、両者は `visit_js_ts_export_clause_specifiers` visitor で AST walk を共用)。ローカル定義を別モジュールへ切り出し re-export を残すリファクタの誤検出を防ぎ、`reconcile_with_moves` (move 相殺、name+kind+signature 完全一致) とは独立に効く — 定義移動時に定義形も変わる (union literal → `keyof MessageMap` 等) と move 相殺は signature 不一致で不成立のため、この抑制が唯一の防御になる。`export * from` (wildcard) は静的に名前を解決できないため抑制対象外 (fail-open 回避)。`filter_exported_symbols` の `pub(crate)` 除外テキスト判定は Rust 限定（他言語の `pub` という名前の関数が API 面から消えるのを防ぐ）。Rust の**モジュール再エクスポート** (`pub use internal::wifi as api;` 等) は `compute_reexport_reachable_modules` が `pub mod` 経路の public_modules を seed に「Named edge の source が到達可能なら target module とその pub 子孫も到達可能」の固定点で `reachable_modules` を構築し、`exposes_symbol` はこの集合で公開判定する（旧実装は resolved_prefix が空の use edge を silent drop しており、module 再エクスポート経由の pub fn 削除・変更が完全無音になっていた）。到達可能化の条件は target が (a) `collect_all_modules` = private にも潜る全 module walk に実在し、(b) **親 module で `pub mod` と宣言されている**こと — private module 自体の再エクスポート (`mod wifi; pub use self::wifi as api;`) は rustc の E0365 で無効なため API 面にしない。module walk の子ファイル解決は `child_module_base_dir` で lib.rs/main.rs/mod.rs (自身の dir) と file-style module (`internal.rs` → `internal/` 配下) を区別し、walk 再帰では解決基準 dir を引数で伝播して inline module へ潜るたびに module 名を積む — `mod internal { pub mod api; }` の api は `internal/api.rs` (旧実装は parent dir 固定で階層 module を取りこぼしていた。既存 `collect_public_pub_mods` の同バグも修正)。`has_cross_file_refs` は bare 名で照合（qualname `Container.method` は identifier に一致せず常に 0 件となり「自ファイル内で呼ばれているだけ」で api.mod 除外が成立してしまうため）。`detect_python_property_to_field` は削除元ファイルが Python の場合のみ適用。非破壊的な互換変更は `compatible_modified` に分離し、React HOC ラップ、未参照 object member 削除（destructuring `const { x } = config` も member 利用として数え、実利用のある削除は blocking 維持。さらに**両側に残存するキーの値**が変わっていれば blocking へ戻す — キー集合の差分だけを見ていると `{a: () => {}}` → `{a: 42, b: () => {}}` のように「既存キーの値の差し替え」と「無関係なキー追加」が同一 diff に同居した場合に追加のみの変更として降格し、呼び出し側の `a()` が実行時に壊れる。値の照合単位は flat object なら top-level キー、record なら **(record キー, member キー)** の粒度で行う＝entry 全体をまとめて比較すると member キーの追加/削除だけで差分になり、本判定の目的である「未参照 member キー削除の降格」が成立しなくなる。値テキストは空白の連なりを 1 個に畳んで正規化し、整形差分だけで blocking へ倒れないようにする）、TS/TSX トップレベル関数の末尾 optional/default 引数追加 (`trailing_optional_params`)、Python トップレベル関数 / モジュール直下クラスメソッドの末尾 kwonly+default / 末尾 positional default 引数追加 (`trailing_optional_params`、`commands/api_changes/python_signature.rs`、デコレータ差分や同名関数複数定義は false negative 回避のため保守的に blocking 維持) と、それらと同じシンボルに紐づく hook `impacts` は Stop hook の blocking 対象から外す。cross-file 参照判定 (`has_cross_file_refs` / `is_used_in_diff_paths` / `is_removed_symbol_unreferenced` / `is_modified_closed_in_diff` / `has_blocking_value_usage`) は `detect_api_changes` 前段 (Phase 0) で候補 name を収集し **ApiRefIndex** (`find_references_batch`、単一 AC + ファイル毎 1 回 parse) に集約して per-symbol の全リポジトリ走査を 1 回のディレクトリ走査に削減。per-symbol 時代の短絡 (`is_internally_connected` で skip された候補は検索されない、removed は同ファイルに new シンボルがある場合のみ) を Phase 0 で再現して過剰収集による AC ヒット拡大を抑え、収集漏れ・batch 失敗の name は `refs_for` が `None` を返して検索失敗時と同じ保守側ポリシー (cross_file/blocking_value→true、他→false) に倒れる (false negative を起こさない)。`is_modified_closed_in_diff` の per-file キャッシュ (import 行集合 / 実変更行集合 / shadow 解決用 parse 結果) は `detect_api_changes` スコープで 1 度確保した `ApiClosureCaches` を全 modified シンボル横断で共有し、per-symbol の `git diff` 起動と tree-sitter parse を unique file 単位 (O(F)) に削減する (v26.6.110、`classify_signature_change` → `is_modified_closed_in_diff` を `&mut ApiClosureCaches` で連鎖)。`is_modified_closed_in_diff` (全 cross-file 参照が同一 diff の実変更行で追随済みの api.mod を `modified_closed_in_diff` = informational に降格) は同名定義が複数あっても、**変更対象ファイル内の定義が 1 つに特定できれば** JS/TS/TSX のトップレベル関数 (bare 名) に限り閉包判定を続行する — 他ファイルの参照は `js_ts_shadow::resolve_reference_binding` (lexical scope chain を内→外に辿り、最初の同名 value binding が同一ファイルの function_declaration の場合のみ除外。property 位置・変数/parameter/import/class binding・解決失敗は除外しない fail-closed。switch_body はスコープとして扱い、for-of/for-in の loop 変数は `for_in_statement` の left field (bare pattern) を scope binding として拾い、`for (var x of xs)` の var ヘッダは関数 scope へ hoist して拾う — ヘッダ binding を見ないと loop 変数への参照を外側の同名 function_declaration へ誤解決し fail-open になる) でローカル呼び出しを除外する。また識別子行が未変更でも、参照が call の callee なら enclosing call_expression の行範囲と実変更行の交差で追随を判定する (複数行呼び出し `startRecording({\n  cursor,\n})` の引数内のみ変更されるケース)。さらに**呼び出し式が完全に無変更**でも、`buildX(SHARED_DEPS)` のように渡している共有 `const` の**定義側**が同一 diff で更新されているケースは追随済みとみなす (`ts_const_arg.rs::closed_via_local_const_argument`、Issue 2026-08-05-api-mod-callers-updated-indirectly パターン C)。既存判定と同程度の証拠強度を保つためガードは意図的に狭く、**すべて**満たす場合のみ: signature 変更が「object type literal 引数への**必須**プロパティ追加のみ」で変更引数が 1 つに特定できる (`ts_signature.rs::detect_added_required_object_props`、optional 追加やメンバー削除・型変更の混在は不成立) / 参照ファイルが TS・TSX / 参照が call の**直接** callee (`ns.foo()` の member 経由は receiver 解決が別問題なので対象外) / 対応する実引数が**裸の identifier** 1 個 (spread が混ざると仮引数との対応が崩れるため不成立) / その identifier が**同一ファイル内**で `const NAME = { ... }` (直接の object literal、`as const`・冗長括弧は透過) に**一意**に束縛されている (同名 binding が他に 1 つでもあれば shadow の可能性で不成立)。binding の列挙単位は「identifier とその親の field 名」ではなく **binding を導入する構文ノード**。パターン (destructuring / rest / default) の再帰と import 判定は `js_ts_shadow::pattern_binds_name` / `import_binds_name` を**共有**し、パターンを持つもの (`variable_declarator` の name / `required_parameter`・`optional_parameter` の pattern / `catch_clause`・bare arrow の parameter / `formal_parameters` 直下の bare pattern / `for_in_statement` の left / `assignment_expression`・`augmented_assignment_expression` の left) を個別 arm で扱い、**それ以外は「`name` field を持つノード」として構造的に**拾う (`name` field を持たず先頭 named child がローカル名になる `import_alias` / `import_require_clause` だけ個別 arm)。名前の照合は `name` field 全体のテキストではなく**左端 identifier** (`leftmost_identifier_text`) — `namespace X.Legacy {}` の name は `nested_identifier` で、完全一致だとローカルに入る `X` を取りこぼす。ここは**取りこぼし = shadow 見逃し = fail-open** で、kind の明示列挙は実際に 3 度穴を開けた: (1) 親 field 名 (`name`/`pattern`/`left`) だけの初版 → bare arrow パラメータ・catch パラメータ・renamed destructuring (`{ deps: X }` の `pair_pattern` value)・配列/rest destructuring、(2) 宣言 kind を列挙した第 2 版 → `using X = acquire()` (tree-sitter-typescript に専用ノードが無く匿名 `using` トークン付きの **`assignment_expression`**) と `abstract_class_declaration` (name が `type_identifier`)、(3) `_declaration` 接尾辞で拾った第 3 版 → `import_alias` / `import_require_clause` (`import X = NS.Value` / `import X = require(..)`) と `function_signature` (`declare function X()`、接尾辞に載らない)。**列挙をやめて構造 (`name` field の有無) で拾う**のが根治で、型空間だけの宣言や class member まで数える過剰検出になるが、過剰側は一意性チェックで不成立 = blocking 維持に倒れるだけで安全。逆に `pattern_binds_name` は `assignment_pattern` / `object_assignment_pattern` を **left のみ**辿る — 右辺 (default 値) まで辿ると `function run(other = SHARED_DEPS)` の参照を parameter binding と誤認し、降格できるケースを blocking に残す false positive になる (`formal_parameters` arm も TS wrapper の `required_parameter` / `optional_parameter` を除外して二重走査しない)。回帰は `detect_api_changes_ts_shadowing_binding_forms_stay_modified` (11 形態 + 「shadow を外せば降格する」**対照ケース内蔵** — 対照が無いと別理由で modified に留まったテストが素通りする。実際に初版テストは実引数を `X as never` にしていて「bare identifier でない」という別理由で通っていた) と `detect_api_changes_ts_const_arg_in_default_value_is_not_a_shadow`。ノード名は `to_sexp()` で実ダンプして確認すること / `let`・`var` は再代入されうるので対象外 / 追加された必須プロパティが**すべて**その object literal にあり、かつ各プロパティの行が**実変更行**に含まれる (キー集合の一致だけでは「元からあった」ケースを区別できない)。import された定数・spread・alias・factory 呼び出しの 1 hop 以上の追跡は module resolution / re-export / 循環 import まで責務が広がるため対象外。**呼び出し参照 0 件のシンボル**は `mod_closed` に降格しない — 参照 0 件は「未使用」ではなく「静的に解決できた内部参照が 0 件」でしかなく、外部リポジトリからの利用・動的呼び出し・文字列参照と区別できない (公開ライブラリではむしろ最重要の破壊的変更) ため blocking な `api.mod` のまま。ただし出力だけでは「更新すべき呼び出し側が本当に無い」のか判別できずトリアージが空振りするため、`ApiSymbolChange::no_resolved_internal_callers` (hook 出力では `no_callers`、false なら省略) をフラグとして添える＝分類も blocking も変えず探索段階だけ省かせる (`ApiSymbol::refs_internal` = `ri` と同じ方針。カテゴリ分離 `mod_no_callers` は「caller が存在しない」と断定しすぎ、かつ既存 hook consumer が `api.mod` を見落とすため採らない。Issue 2026-08-05-api-mod-callers-updated-indirectly パターン B)。method qualname と JS/TS 以外の言語は従来ガード (同名複数定義 → 即 blocking) を維持 (Issue 2026-07-12-api-mod-same-diff-informational)。`--hook` 出力の `api.add` には抽出条件の自己記述 `add_scope: "no_cross_file_refs_in_diff"` を付ける（`add` が非空のときだけ出力）— `add` に載るのは「新規 export のうち同一 diff 内でまだどこからも参照されていないもの」だけで、diff 内で既に他ファイルから使われる新規 export は載らない。キー名だけでは「追加した export 全部」と読めるため、載らない新規 export を検出漏れと誤認したトリアージが実際に走った (Issue 2026-07-27-api-add-scope-not-visible)。キー名自体の変更は既存 hook / 後続ツールの JSON パースを壊すため採らない。`add_scope` の値は抽出**範囲**しか表せずシンボル単位の参照状況は表現できない — 実際の抽出条件は「同一ファイル内の**呼び出し (callee)** 参照が無い」+「同一 diff の他ファイルからの実利用参照が無い」の合成で、TS の型注釈のように呼び出しでない同一ファイル内参照は条件に現れないため、`export interface X` を同一ファイル内の型注釈からのみ参照するケースが「diff 内でどこからも参照されていない = デッドコード」と読み違えられ、トリアージが `refs` を再実行する事故が起きた (Issue 2026-08-04-review-add-scope-naming)。値の改名では 2 条件の合成を 1 語で表せないため、`api.add` の各シンボルに同一ファイル内の実利用参照数 `refs_internal` (hook 出力では `ri`、0 なら省略) を添えて解決する＝`ri` が付けば「同一ファイル内では使われているが他ファイルからは参照されていない」、付かなければ「実利用参照ゼロ」と読める。**それでも旧値 `unreferenced_in_diff` は残しておけなかった** — `ri` を添えた後も「`ri` が非ゼロなのにラベルが `unreferenced_` を名乗る」という同じ誤読が 2 度再発した (2026-08-10 / 2026-08-11)。`ri` は個々のシンボルの補足であって、ラベル自体が実態と矛盾している事実は消せない。そこで**キー名 `add_scope` は維持したまま値だけを `no_cross_file_refs_in_diff` に変えた** (v26.8.106)＝キーで分岐する既存 consumer の JSON パースは壊さず、値を読んだときに「同一 diff の他ファイルから参照されていない」という実際の抽出範囲が伝わる。値を厳密比較する consumer は影響を受けるが、矛盾した値を恒久的に維持するより controlled な契約変更を選ぶ。2 条件のうち「同一ファイル内の呼び出し参照が無い」側は依然として値では表せないため、`ri` との併用は継続する。算出は `ref_index.rs::count_internal_refs` が構築済みの `ApiRefIndex` から引くため追加のディレクトリ走査ゼロで、`reconcile_with_moves` 後の api.add 確定候補にだけ 1 度行う (move 相殺で抜けた候補には算出しない)。定義行・import/use 行・named export clause (`export { X }` / `export type { X }`) は実利用ではないため除外する (`export type Foo = Bar` のような型エイリアス定義は `{` が来ないため除外せず `Bar` を実利用として数える)。複数行 export clause の継続行は行テキストから re-export と判定できず実利用として数えるが、過大側 = 「内部参照あり = export を残す」判断に倒れるため安全側。値の細分化 (`ref_status`) と公開 API シグネチャ経由の到達可能性 (`in_api_signature`) は、api.add が既に非 blocking で `ri` により目的を達成できるため採らない (TS のジェネリクス / ユーティリティ型まで型解決を追うと実装が重く fail-open リスクを負う)
- **api.rm の残存参照帰属 (言語互換 + Python 属性アクセス)** — 削除シンボルの残存参照は **bare name** で数えるため、polyglot リポジトリでは無関係な他言語の同名シンボルへの参照が混入し、呼び出し元ごとアトミックに削除したモジュールまで blocking な `api.rm` に残っていた（Issue 2026-08-06-api-rm-atomic-module-deletion。実測では bare name `search` の参照 2,522 件のうち 2,521 件が PHP 1,739 / JS 575 / C 146 / TS 53 等の他言語で、削除した Python 関数へ到達する経路は 1 件も無かった）。`proves_survivor_origin` は帰属証明の前段で **参照ファイルの言語から削除ファイルの言語へ識別子束縛の経路があるか** を `LangId::can_reference_definition_in` で判定し、経路が無ければ「同名でも別物」として証明を成立させる。判定は**方向を持つ**（参照側 → 定義側）: Python の ctypes (`lib.foo()`) は C の定義を識別子として呼べるが、逆に C から Python のモジュールレベル関数を識別子で呼ぶ経路は無い（CPython 埋め込み API は文字列名で解決する）。対称な「言語グループ一致」で判定するとこの非対称を潰し、Python 削除に対する C の同名参照まで互換扱いになって修正が効かなくなる。一次判定は `Ecosystem`（JS/TS/TSX・C/C++・Java/Kotlin が各 1 グループ、`_` catch-all を置かず `LangId` を全列挙）の一致で、エコシステムをまたぐ唯一の識別子束縛経路として `def_lang.exports_c_abi() && ref_lang.imports_c_abi()` を許す。JNI (`Java_<pkg>_<class>_<method>` へマングル) / Java FFM / Ruby Fiddle / `dlsym` は**文字列名・マングル名**で解決するため識別子束縛ではなく対象外。参照ファイルの言語は拡張子 → shebang の順で解決し（拡張子なしシェルスクリプトも参照元になり得る）、`.html` は Angular component template 走査経路でしか参照を出さずその識別子は TS component クラスのメンバに解決されるため TS 扱いにする。言語が解決できない参照（Android XML 等）は fail-closed で証明しない。残る同一言語の参照については Python 限定で、symbol の出現が**すべて属性アクセス**（`re.search` / `self.search` / `pkg.mod.search`）ならレシーバの実効モジュール名（末尾 identifier を `as` 別名で解決したもの）に削除モジュール名（`pkg/core.py` → `core`、`pkg/__init__.py` → `pkg`）が無い限り証明を成立させる（`RefOrigin::PythonAttributeAccess`）。bare identifier の出現が 1 件でもあれば（`from core import search` の import 行、`search()` の直接呼び出し）証明を諦める。**メソッド削除（`BM25.fit` / kind == "method"）では属性アクセスこそが正当な参照形なので証明しない**（fail-closed）。回帰は `proves_survivor_origin_uses_cross_language_incompatibility`（非互換言語で証明成立 / 同一言語・言語不明・C ABI 公開言語の削除では blocking 維持の**対照ケース内蔵**）/ `proves_survivor_origin_python_attribute_access` / `analyze_python_facts_detects_attribute_only_usage` / E2E の `detect_api_changes_atomic_python_module_deletion_demoted_to_removed_dead` と `..._keeps_blocking_on_residual_python_ref`
- **循環的複雑度** — symbols 出力に `cx`（cyclomatic complexity）を付加（関数/メソッドのみ、ベース1 + 判定点数、ネスト関数/クロージャは除外）。**計上規約は McCabe に揃え、同じロジックが言語をまたいで同じ値になることを要件とする**（しきい値 `cx > 10` 等での判断と polyglot リポジトリでの比較が成立しないため）。規約は 3 点: (1) switch/match は **arm だけ**を数え構文本体は数えない（3 分岐 switch は cx=4）、(2) plain `else` は数えない（`else if` はネストした `if` として自然に +1 される）、(3) 三項演算子は数える。catch-all arm（`default:` / `_ =>` / `else ->`）を数えるかは文法依存で ±1 のぶれが残る（別ノードを持つ Go `default_case` / JS `switch_default` / PHP `default_statement` / Ruby `else` は数えず、arm と同一ノードになる Rust/Kotlin/Python/Swift/Java/C#/C では数える）。分岐ノードは **named ノードのみ**計上する（tree-sitter-ruby は `if` 文ノードとキーワードトークン `if` が同名 kind のため、named 制約が無いと約 2 倍になる）。**分岐ノードテーブル (`branch_node_kinds`) / ネスト境界テーブル (`function_boundary_kinds`) はどちらも `_` catch-all を置かず `LangId` を全列挙する** — 汎用スライスの流用や grammar の改称に追随できていない場合、エラーにならず「1 つもマッチせず cx=1 のまま」という無害に見える壊れ方をするため。実測で判明した不整合を v26.7 で一括修正: Rust/Zig が plain `else_clause` を計上（if/else が cx=3）、C/C++/Go/PHP/Python/Kotlin/Swift/Ruby/bash/Zig/Rust が switch 本体と arm を二重計上（3 分岐で cx=5）、Java は逆に arm を 1 つも数えず 3 分岐が cx=2、PHP の `else_if_clause`・PHP/Java/C#/Ruby の三項演算子（`conditional_expression` / `ternary_expression` / `conditional`）が欠落、C# の foreach がノード名誤り（`for_each_statement` は存在せず正しくは `foreach_statement`）。回帰は `cx_*_in_every_language` 系テストが同一ロジックを全 tree-sitter 言語で並べて期待値に固定する
- **Go メソッドのレシーバ帰属** — Go の `func (b Box) Area()` はレシーバ型が container だが、宣言はトップレベルに並ぶため range 内包ベースの `assign_enclosing_containers` では引き当てられない。`symbols/mod.rs::go_receiver_type_name` が `method_declaration` の receiver から型名を取り（`*Box` / `Box[T]` も型名だけに正規化）、抽出時に container を確定させる。`ExportSurfaceContext::qualname` は `Symbol.container` が設定済みならそれを優先する。未設定のままだと `Alpha.Value` / `Beta.Value` が両方 bare `Value` に潰れ、API 差分の突き合わせで別物同士が混ざる
- **設定ファイル** — `~/.config/astro-sight/config.toml`（TOML 形式、`astro-sight init` で生成、`log_path` 未指定時は config ファイル隣の `logs/`、明示指定時は同じ値でも尊重）
- **ロギング** — logroller による日次ローテーション（ローカルタイムゾーン、3日保持）。debug 時のログ初期化失敗（書込不可ディレクトリ等）は警告に留めて解析本体は継続する（read-only 環境などへの堅牢化）
- **`--git` の base セマンティクスを全コマンドで統一** — `context` / `impact` / `review` / `dead-code` / `cochange` はいずれも `git diff <base>` の**単一 revision 形式**で評価し、既定 base は `HEAD`（= 未コミットの作業ツリー変更）。旧 cochange だけ `git diff <base> HEAD` の 2 revision 形式 + 既定 `HEAD~1` で、「`--git` を付けても未コミット変更を一切見ない」「同じ `--git` で review と cochange が別の diff を見る」状態だった。`HEAD~1` を明示すると「直前コミット + 未コミット変更」になる。この統一により、pre-commit レビュー（未コミット変更）でも cochange の変更行 blame が機能する。作業ツリー比較になったぶん退化した tree（no-checkout worktree、rebase 途中の大量削除など）では追跡ファイル全件が起点に化けうるため、`--max-source-files` の既定を 0（無制限）から 500 に変更して暴走を明示エラーで止める（実レビューの diff は数十ファイル規模なので当たらない）
- **git 由来パスの基準を `--dir` (ワークスペース) 相対に統一** — astro-sight の内部パス規約は一貫して `--dir` 相対（参照 index、`Path::new(dir).join(..)` のファイル読み込み、`is_contained_relative` のサンドボックス判定、JSON 出力）だが、**git サブコマンドごとに既定の出力基準が違う**: `git diff` / `git diff-tree` はリポジトリルート相対、`git ls-files` / `git ls-tree` は cwd 相対、`git show <rev>:<path>` はリポジトリルート解決。`--dir` がリポジトリルートなら全部一致するため長く露見しなかったが、サブディレクトリ（frontend が `src/`・backend が `app/src/` のような 2 プロジェクト構成で backend 側を `--dir` にする等）では**同一レコード内で基準が混ざる** — `api.moved` の `from`（削除側 = git diff 由来 = ルート相対）と `to`（追加側 = `git ls-files` 由来 = dir 相対）が食い違い、`to` がリポジトリルートから見て実在しないパスになっていた（Issue 2026-08-05-moved-name-only-match-and-path-mismatch のパターン B）。合わせて、diff 由来のルート相対パスを `dir.join()` で読む経路が全滅（`app/app/src/...` になる）し、base 側だけ `git show` で読めるという非対称から**誤った api.rm / rm_dead が大量発生**していた（実測 rm 3 / rm_dead 60 が全て公開 API 面判定の失敗による FP）。dir 相対側に揃える（ルート相対に揃えない）のは、`--dir` が CLI 上「Workspace directory」であり解析エンジン全体がその前提で書かれているため。修正は「基準を出す側」に閉じる: `git diff` 系（`run_git_diff_collecting_truncations` / `resolve_blame_source_files` / `ref_index.rs::changed_new_lines_for_file`）に `--relative` を付け、逆にルート解決する `git show` は `git_input.rs::git_show_blob` が `<rev>:./<path>` 形式（`cwd_relative_git_path`）で cwd 基準に固定する＝`git show` の唯一の実行点なので変換もここ 1 箇所に閉じる。`git ls-files` / `git ls-tree` は元から cwd 相対なので変更しない。**`cochange.rs::collect_files_in_commit` の `git diff-tree` にだけは `--relative` を付けない** — `--relative` はパス変換だけでなく `dir` 配下外を git 側で落とすため、コミット規模 (`total_files`) まで「ワークスペース内の変更ファイル数」に化け、`max_files_per_commit` のノイズ除外と `commit_size_weight` の意味が壊れる（リポジトリ全体 500 ファイルの一括更新が、ワークスペース内 2 ファイルの集中コミットとして高スコアを作りうる）。ここはルート相対で全件受け取って規模はその件数で評価し、共変更相手として返すパスだけ `workspace_prefix_for`（`git rev-parse --show-prefix`、SHA ごとに起動しないよう 1 度だけ解決）で prefix を剥がして `dir` 相対に揃える（`CommitFiles { total_files, workspace_files }`）。`workspace_prefix_for` の失敗は **fail-closed**（エラー伝播）— 空文字にフォールバックすると、サブディレクトリ実行なのに全ルート相対パスが候補に入り、`source_set` の起点除外と `exclude_matcher` が別基準で評価されて起点自身（`app/a.rs`）が共変更相手として返りうる＝統一したはずの基準がここだけ破れる。「成功かつ空出力」＝リポジトリルートのときだけ空 prefix として扱う。回帰は `cochange_from_subdirectory_uses_repo_wide_commit_size`（規模超過の除外 / prefix 剥がし / 起点自身が候補にならないこと）。**`--dir` = リポジトリルートでは prefix が空になり `--relative` も `./` も no-op** なので、既存の主運用の出力は完全に不変（`run_git_diff_at_repo_root_keeps_repository_relative_paths` で固定）。副作用として `--dir` 配下外のファイルは解析対象から外れるが、これは配下外を参照解決もファイル読み込みもできない以上「ワークスペース = `--dir`」の規約どおりの挙動。回帰は `run_git_diff_from_subdirectory_uses_workspace_relative_paths` / `git_show_blob_resolves_paths_relative_to_workspace_dir` / integration の `moved_paths_share_workspace_basis_from_subdirectory`（`from` / `to` が同一基準かつ `to` が実在することを固定）
- **git 管理外ディレクトリの graceful skip** — `--git` を受けるコマンド（context / impact / review / dead-code / cochange）が git 管理外 dir で実行された場合、`git rev-parse --is-inside-work-tree`（`LC_ALL=C`）で事前判定し「解析対象なし」として **exit 0** で skip。`--hook`（review / impact）は完全無出力（silent skip）、通常 CLI は各結果型に `skipped: Option<SkipInfo>`（`reason="not_git_repository"` / `source="git"` / `message`）を付けた空結果を返し「差分なし」と「git 管理外」を区別可能にする。真のエラー（base 不正・git 実行不能・壊れた repo・権限）は従来どおり exit 1（fail-closed）。判定は `commands.rs` の `resolve_git_diff`（経路A: context/impact/review/dead-code）と `resolve_blame_source_files`→`BlameSourceResolution`（経路B: cochange、`--paths`/`--paths-file` 明示時は管理外でも明示分を尊重）に集約。`--git` 指定で差分が空 / 全件が除外 glob（vendor / dist / ロック等）に該当した場合も入力エラーではなく空結果 exit 0 に倒す（生成物だけを触ったコミットで cochange / review 全体が落ちない）。起点の指定手段が 1 つも無いとき（`--git` なし + `--paths` なし）だけ InvalidRequest。impact は構造化 JSON 出力を持たないため skip も無出力 exit 0（既存の「差分なし」と一貫）
- **未追跡ファイルの取り込み上限と打ち切り申告** — `--git` (非 `--staged`) は未追跡ソースを「全行追加の新規ファイル」として合成 diff に足す（`append_untracked_added_diffs`。git diff が untracked を出さないため、同一作業で作った未追跡 sibling への参照が「diff 外の未解決影響」と誤報されるのを防ぐ）。ただし **1 ファイル 256KB (`MAX_UNTRACKED_FILE_SIZE`) または 5,000 行 (`MAX_UNTRACKED_FILE_LINES`) を超える未追跡は合成しない**。生成物の全 exported symbol が api.add 候補になると `detect_api_changes` の Phase 0 が数万 name を ApiRefIndex に渡し、review が数十分かかって Stop hook (120 秒) がタイムアウトして**沈黙**する（実測: 未追跡 22,000 `pub fn` で 1.75 秒 → 10 分超で未完了。上限導入後は同条件で 3.98 秒。Issue 2026-08-04-review-git-untracked-huge-file-blowup）。サイズは metadata で read 前に、行数は読み込み後に判定する（1 行が短い生成コードはサイズ上限に達しないまま数万シンボルを持つため両方必要、OR 条件）。tracked には適用しない＝commit / add 済みは意図的にレビュー対象に入れたものと見なせる一方、未追跡は「まだ add していない」= コミット対象か不明で巨大なら生成物の可能性が高い（`append_untracked_added_diffs` 自体が既に「unstaged のみ / staged と `--diff-file` は対象外」という非対称を持つ、その延長）。除外は **silent にしない**（AGENTS.md の No silent caps 原則）: `models/truncation.rs::TruncationInfo` (`path` / `reason` / `message`、閾値と実測値を message に含む) を `ContextResult` / `ReviewResult` / `DeadCodeResult` の `truncations` に載せ、hook 出力では `trunc: [{f,r}]` として出す（blocking にはしない＝検出ではなく解析範囲の申告だが、沈黙させると「全部見た」と読める）。`skipped: SkipInfo` (コマンド全体を解析しなかった大域 skip) とは別フィールドにする＝`skipped` を拡張すると「差分なし / 解析未実行」と誤読され、打ち切りが起きたのに「レビュー済み」と読めてしまう。伝播は `GitDiffInput::Diff { diff, truncations }` / `DiffSourceResolution::Diff { diff, truncations }` の struct variant で値返しにして、伝播漏れがコンパイルエラーになるようにする（out-param だと渡し忘れても動き、打ち切りが黙殺される）。inline `--diff` / `--diff-file` は明示範囲を尊重するため常に空。impact は構造化 JSON を持たないため stderr の `note:` 行で出す。**上限超過ファイルは untracked リストから落とさず `UntrackedSourceFile::oversized` で印を付けるだけにし、rename ペアリングの相手としては残す** — 落として deleted 側だけを残すと「巨大ファイルを rename しただけ」で旧ファイルの全 exported symbol が api.rm / rm_dead に流れ込む（実測 5,001 シンボル / 135KB の hook 出力。上限導入時に一度作り込んだリグレッション）。ペア成立時は `synthesize_modified_file_diff` を呼ばず `synth=None` に倒して削除 block の除去だけ行う＝「rename は認識するが中身は見ない」（内容同一 rename の既存経路と同じ形）。未 pair の oversized は合成しない。回帰は `resolve_git_diff_oversized_untracked_rename_suppresses_deletion_block` が削除 block 除去と `detect_api_changes` の removed / removed_dead 空を両方固定する
- **未追跡 rename ペアリング** — unstaged の「ファイル削除 + 同内容の untracked」を high-confidence rename として合成 diff (`Modified{old,new}`) に正規化する（`git_input.rs::pair_untracked_renames`）。構造 gate は同一言語・サイズ比 2 倍以内・exported symbol 集合 (name,kind,signature) の完全一致で、両端 degree 1 の一意対応のみ採用（ambiguous は fail-closed で pair しない）。exported symbol の抽出は per-file の `RenameFacts` として **D+U 回だけ**事前計算し、相手側集合に同じ言語が無いファイルは parse 自体を省く（旧実装は D×U の全組合せで gate 内フルパースし、ディレクトリ移動 D=U=200 で数万回の再パースが起きていた。出力は旧実装と完全一致）

## Key Modules

- `src/service.rs` - **AppService**: 全コア操作の統一エントリポイント（extract_ast/symbols/calls/imports/lint/sequence/cochange, find_references/find_references_batch, analyze_context + パス検証 + cochange オプション検証 + tracing ログ）
- `src/output/mod.rs` - **OutputOptions**: `--format` / `--pretty` / `config.toml` の解決結果と `serialize_document` / `serialize_toon_list_item`。プロトコル面 (session / hook / エラー) の JSON 固定判定 (`ensure_json_protocol`) もここ
- `src/output/toon/` - TOON v4.1 encoder (`mod.rs` = 公開 API、`value.rs` = 順序保持の中間表現 + `serde::Serializer`、`scalar.rs` = quoting / escape / canonical number、`encode.rs` = object / inline array / tabular / keyed tabular / list form、`tests.rs` = 仕様適合テスト)。decoder は持たない。delimiter は comma 固定、indent は 2 スペース固定
- `src/output/nullable.rs` - astro-sight DTO 向けの欠損 nullable 列正規化 (tabular 化のための `null` 補完。厳密エンコードより短い場合だけ採用)
- `src/skill.rs` - スキルインストール（`skill-install claude/codex` → ~/.claude/skills/ or ~/.codex/skills/）
- `src/config.rs` - 設定ファイル管理（ConfigService: load/generate、TOML 形式、`log_path` の未指定と明示指定を区別）
- `src/logger.rs` - ロギング（logroller 日次ローテーション、3日保持、tracing-subscriber）
- `src/cli.rs` - CLI サブコマンド定義（ast, symbols, calls, refs, context, impact, review, dead-code, imports, lint, sequence, cochange, doctor, session, mcp, init）
- `src/main.rs` - コマンドディスパッチ、キャッシュ層、バッチ処理（全て AppService 経由）、`batch_ndjson` はワーカー数の 8 倍を上限とする順序保持 chunk で並列処理し、未排出結果を全件保持しない。CLI stdout の broken pipe は panic 表示を抑止し、`head` 等の通常パイプ利用を正常終了扱いにする
- `src/commands/api_changes.rs` - API 差分検出のオーケストレーション（`detect_api_changes`: Phase 0 で exported シンボル抽出 + ApiRefIndex 構築 → process_added/deleted/modified_file、および `reconcile_with_moves`）。実処理は責務別サブモジュールへ分割済み: `prepare.rs`（diff 前処理 = `should_skip_diff_file` / `PreparedDiffFile` / `extract_new_file_facts` と、ファイル単位の分類関数が共有するコンテキスト型）、`diff_processing.rs`（`process_added_file` / `process_deleted_file` / `process_modified_file` / `classify_signature_change`）、`exported.rs`（公開 API 面の抽出とフィルタ = `ExportSurfaceContext` / `filter_exported_symbols`）、`signature.rs`（シグネチャ抽出・正規化と binding shape）、`removed.rs`（`partition_removed_dead_candidates` と外部 import / bash / Python の帰属判定）。分類関数の引数は immutable な `DetectionInputs`（dir / base / diff_files / diff_new_paths / ref_index）と mutable な `DetectionState`（buckets / Rust 再エクスポートキャッシュ 2 種 / closure_caches）に分ける＝「読むだけの入力」と「書き換える先」を型で区別し、`&str` や `&mut` キャッシュが 14 個並ぶ位置引数の取り違えを防ぐ（`CmdAstOpts` と同じ方針。本体先頭で一括分解して従来の局所変数名を維持する）。ファイル単位の事実は `AddedFileFacts` / `ModifiedFileFacts`、シグネチャ変更 1 件は `SignatureChangeSite` で束ねる。なお missing_cochange 警告の生成（`detect_missing_cochanges` / `is_dependency_manifest_pair`）は API 差分検出ではなく review 側の責務のため `src/commands/review/missing_cochange.rs` に置く。Phase 0 は `extract_new_file_facts` で new_path を **1 回だけ read+parse** して exported/callees/export_surface_names の 3 facts を導出（旧実装は同一 new_path を TS/JS/Rust で 3 回・他言語で 2 回 parse、各抽出のガード=test path/`is_safe_diff_path`/lexer-only/言語別 export surface を厳密維持）、export_surface_names (TS/JS named export clause + Rust `pub use` の公開名) は `PreparedDiffFile::Modified` に先取り保持して process_modified_file の再 parse を排除。公開面のフィルタは `ExportSurfaceContext` にファイル単位の不変情報を集約し、定義/API要素/実行時入口/qualname 規約の純粋な除外判定へ分割する。PHPUnit・constructor・Flyway は API 差分と dead-code の両方、Laravel・Angular lifecycle は dead-code のみという既存境界を一律化しない。互換判定器 6 個 (TS 5 + Python 1) の共通入力は `source_pair.rs` の `CompatibleModSite`（`dir` / `base` / `old_path` / `new_path` / `name` / `kind` / `old_sig` / `new_sig` / `lang_id` の 9 項目。同型 `&str` の `old_path`/`new_path`・`old_sig`/`new_sig` を位置引数で渡すと取り違えてもコンパイルが通るため構造体で束ねる）で、old/new ソース取得は `SignatureSourceCache` が遅延 + メモ化する（旧実装は判定器ごとに `git show` を起動し 1 シンボルで最大 4 プロセス spawn。遅延なのは言語ゲートや安価な pre-gate で全判定器が弾かれる場合＝Rust の api.mod 等に `git show` を 1 度も起動しない挙動を保つため。実測: 全 TS 判定器を通す diff で `git show` 6→3 回）。サブモジュール: `rust_public.rs`（Rust 公開 API 面判定 / re-export edge graph、`RustBaseReexportCache` が base 側 crate 判定 `is_binary_only_rust_crate_at_base` / `rust_private_module_info` を old_path 単位でメモ化し per-symbol の多重 `git show` を排除＝dir/base は detect_api_changes 呼び出し内で固定。pub 到達 module 集合は `pub_reachable_modules` が `collect_all_modules` の pub 子リストから導出する＝module ツリー walk は 1 回のみ。旧実装は pub 経路専用 walk (`public_reachable_modules` + `collect_public_pub_mods`) を別に持ち、file-style module の子解決を片方だけ直して階層 module を取りこぼした経緯がある）、`ts_signature.rs`（TS/JS シグネチャ・React HOC / object member / 末尾 optional 互換判定）、`python_signature.rs`（Python トップレベル関数 / モジュール直下クラスメソッドの末尾 kwonly+default / 末尾 positional default 互換判定。`find_python_function_by_name` は同名候補が複数あれば `None` を返し blocking 維持、`python_collect_decorators` でデコレータ差分も比較対象に含めて `@staticmethod` ↔ `@classmethod` のような bind 変化を取りこぼさない）、`ref_index.rs`（ApiRefIndex + cross-file 参照判定 5 ヘルパー + api.add に添える同一ファイル内実利用参照数 `count_internal_refs`）、`removed_attribution.rs`（api.rm 候補の残存参照帰属。`RefAttribution { ref_lang, origin }` は候補非依存の事実だけを持ち、削除元 `RemovedCandidateRef`（`old_path` / `name` / `kind` を構造体で束ね、同型 `&str` の位置引数取り違えを防ぐ）との照合は `proves_survivor_origin` 側で行う）
- `src/commands/dead_code.rs` - dead-code 検出（`detect_dead_symbols_from_files` は collect 候補 → name index 構築 → framework asset refs 収集 → classify → 宣言行付与 の 5 ステージ分割、規約除外の追加は classify の述語 1 つで閉じる）。`DeadSymbol` には宣言行 `line` を付ける（`attach_declaration_lines` が per-file で `extract_symbol_lines` を 1 度だけ呼ぶ。qualname が引けない class member は bare name で再 lookup）。`file` だけの報告は利用者が結局シンボルを探し直すことになり、識別子検索を AST に置き換えるという本ツールの目的と噛み合わないため。hook 出力も `dead: [{n,f,l}]`
- `src/mcp/mod.rs` - MCP サーバー（AstroSightServer + AppService::sandboxed(cwd) + 11 ツール、fail-closed: sandbox 生成失敗時はパニック）
- `src/engine/parser.rs` - tree-sitter パーサー管理（100MB ファイルサイズ上限、SourceBuf によるゼロコピー mmap、`.h` の C/C++ 自動判定、thread-local 言語別 Parser 再利用）
- `src/engine/query_cache.rs` - tree-sitter Query のプロセス内キャッシュ（`(LangId, クエリ文字列)` 単位、上限 512 件、Query は Send+Sync のため Arc 共有。QueryCursor は状態を持つため対象外）
- `src/engine/bounded_read.rs` - 補助テキストスキャン向けの上限付き UTF-8 reader（metadata 前後の TOCTOU を上限 + 1 byte の再検証で防止）
- `src/engine/extractor.rs` - AST ノード抽出
- `src/engine/symbols/` - シンボル抽出（2026-07-16 に単一 5550 行ファイルから責務別に分割。外部参照パス `crate::engine::symbols::X` は mod.rs の再エクスポートで不変）
  - `mod.rs` - 抽出コア（extract_symbols / run_symbol_query / assign_enclosing_containers / doc 抽出）+ 言語別 tree-sitter クエリ + 共有ヘルパー（node_for_symbol_range / enclosing_of_kind）。**Rust の generic inherent impl** (`impl<T> Holder<T>`) は型名の parent が `generic_type` 止まりで range が型名部分に潰れるため、`impl_item` まで昇格して本体全体を range にする（Ruby の `class A::B` と同型の対処）。昇格しないと `assign_enclosing_containers` の range 内包判定で引き当てられず qualname が bare 名になり、同名メソッドの取り違えとレシーバ型の可視性を使った公開 API 判定の失敗を招く
  - `scope.rs` - ローカルスコープ判定（is_local_scope_symbol + 言語別 has_enclosing_function_body_*）
  - `exported.rs` - export 判定（is_symbol_exported、JS/TS export clause、Rust pub use 再エクスポート収集、collect_js_ts_named_export_surface_names、Python `__all__`）
  - `overrides.rs` - override メソッド判定
  - `framework.rs` - フレームワーク規約の runtime entrypoint 判定（PHP Laravel / Java Flyway / JS-TS DSL / Angular ライフサイクル・CVA・member decorator（is_js_ts_angular_lifecycle_hook: `@Component` / `@Directive` 装飾クラスのメソッドのみ true）/ Python entrypoint デコレータ（has_framework_entrypoint_decorator_python）+ python_class_base_names + urllib.request / watchdog の動的プロトコルメソッド / PHP 擬似 enum）
  - `cpp.rs` - Rust trait impl 判定 + C/C++ helper 群（is_cpp_forward_declaration / is_cpp_nested_function / cpp_enclosing_function_definition / is_exported_cpp）
  - `complexity.rs` - 循環的複雑度（calculate_complexity、言語別 branch/boundary kinds）
- `src/engine/calls.rs` - コールグラフ抽出（言語別 call expression クエリ。Kotlin のメソッド呼び出しは `navigation_suffix` 配下の識別子を callee として捕捉＝receiver 名を callee にしない・チェーン `a.b.c()` も末尾を捕捉、Java は `name:` パターン 1 本で object 有無に依らず捕捉し二重計上しない。JS/TS の arrow / function expression の caller は親の binding 名 — `variable_declarator` の name / object `pair` の key / class field / assignment 左辺 (member は末尾 property) — で解決し、`x => …` の引数名を caller に誤帰属しない。binding が無い匿名 callback は偽名を作らず `find_enclosing_function` が外側の named function へ climb を継続する＝edge 消失を防ぐ）
- `src/engine/refs/` - クロスファイル参照検索（2026-08-06 に単一 4504 行ファイルから責務別に分割。外部参照パス `crate::engine::refs::X` は mod.rs の再エクスポートで不変。`mod.rs` = public API + rayon pool + AC 事前フィルタ、`walker.rs` = `walk_refs` と matcher/sink、`files.rs` = `collect_files` 系、`line_index.rs` = 位置計算、`role.rs` = usage role 分類、`lexer_path.rs` = lexer-only 言語経路、`definition/` = 言語別 definition-context 判定（`mod.rs` の dispatch は `LangId` 全列挙・catch-all なしを維持。**型注釈位置の識別子は Reference**: 汎用の parent/grandparent 走査は `function_definition > type > identifier` (Python の `def f() -> Base`) や `class_definition > argument_list > identifier` (`class Derived(Base)`) を祖父ノード経由で Definition と誤判定し、参照として数えられないため dead-code で「基底クラス」「戻り値型でしか使われないクラス」が dead に、`api.add` の `refs_internal` が過小になっていた。JS/TS・Zig と同じく `is_python_definition_context` が宣言の `name` フィールド一致を要求し、`parameters` 直下の bare パラメータ名だけを例外的に Definition として残す。`global` / `nonlocal` / `except ... as` / walrus / match capture / lambda 引数 / PEP 695 型パラメータは parent・grandparent が定義ノードにならないため従来どおり Reference。**同型の誤判定は汎用パスに落ちる Go / Java / Kotlin / Swift / C# にも残っている**（戻り値型が全言語共通、基底クラスは Java / C#）＝言語ごとに実ノード名とフィールド構成を確認してから同じ形で潰す必要がある）、`tests/` = 散在していた 3 つの `#[cfg(test)]` を集約）（ignore + rayon 並列 + fold/reduce 集約 + memchr/memmem 事前フィルタ + Aho-Corasick バッチ検索 + `collect_files` pub ユーティリティ）。per-file の参照走査は統合 walker `walk_refs<M: RefMatcher, S: RawRefSink>` 1 本に集約 — 旧 single/batch/callback/count の 4 walker が「identifier ガード + 5 種 synthetic ref 源 (rust_attr_string / bash_trap / phpunit_metadata / php_callable_array / php_string_callable) + 再帰」を丸ごとコピペしていた同期ハザードを解消。照合差は `SingleMatcher` (テキスト比較、1 要素 HashMap 化しない) / `IndexedMatcher` (name→ix lookup)、出力差は `SymbolReferenceSink` (Vec 構築、context は 1 hit 最大 1 回抽出) / `VisitorAdapter` (RefVisitEvent 構築、confidence / macro callee / usage role 分類はここのみ) / `CountSink` (加算のみ) の静的ディスパッチに分離し、`RawRefSink::NEEDS_LINE_INDEX` (CountSink=false はコンパイル時 assert) で count 経路の `LineIndex` 構築自体を省く。ref 源の追加先は walk_refs 1 箇所。lexer-only 言語 (Xojo) は `find_refs_via_lexer` / `find_refs_batch_via_lexer` 経由で identifier 走査、行コンテキスト抽出は `LineIndex` でファイル単位の改行位置 index を 1 度だけ構築し、tree-sitter / lexer 両経路 (`extract_line_context_indexed` / `extract_line_context_bytes_indexed`) で参照件数 M に対し per-call O(filesize) → O(M + filesize) に短縮（v26.6.114、3 visitor + 2 lexer 経路で line_index を伝播、後方互換の単発 `extract_line_context*` は `#[cfg(test)]` 専用 wrapper）。256B 上限による巨大行切り詰めは継続。埋め込み領域 (Angular テンプレート / PHPUnit docblock・attribute / bash trap) の「領域内 offset → ファイル絶対位置」変換は `LineIndex::row_col` / `byte_offset_to_row_col` / `absolute_position(base, rel)` の 3 ヘルパーに集約する（**1 行目だけ base 列を加算し 2 行目以降は加算しない**という off-by-one を生みやすい規約を 1 箇所で持つ。旧実装は 3 モジュール 4 箇所で個別に書かれ、bash の引用符 +1 だけ独自ルールだった）
- `src/engine/angular_template_refs.rs` - Angular テンプレート参照抽出。byte スキャナは callback 版 (`for_each_template_expr` / `each_binding_expr` / `for_each_inline_template`) が単一実装で、`extract_*_refs` はその薄いアダプタ（旧実装は dead-code 用と位置付き refs 用に同一スキャナを 4 対コピペしており、`@if` / `@for` 等の制御フロー対応が dead-code 側だけに入るドリフトが起きていた）。位置計算は `LineIndex` 経由（旧実装は hit ごとに offset 0 から再スキャン + `lines().nth(row)` で O(hits × filesize)。実測 24000 hit / 300KB で 486 秒 → 1 秒）。index は遅延構築で 0 hit テンプレートのコストは不変、256B 切り詰めは `...` マーカーを付けない従来仕様を維持。テンプレート / component TS / package.json の読込は 2MB 上限を metadata 後にも再検証する
- `src/engine/lexer/` - 手書き state machine による未サポート言語向け fallback。`LexerProfile` でキーワード/コメント/文字列区切り/定義 keyword を宣言し、`extract_symbols` と `find_identifier_refs` を提供する。Xojo profile を内蔵
- `src/engine/diff.rs` - unified diff パーサー（`HunkProgress` で hunk の old/new 行数を厳密追跡し、本体の `--- a/` / `+++ b/` 風コンテンツ行をファイルヘッダと誤認しない＝以降の本体脱落による false negative を防ぐ。`parse_hunk_header` / `HunkProgress` / `HunkBodyLine` は pub(crate) で signature.rs と共用。`parse_hunk_header` は unified diff 仕様違反 (`count > 0 && start == 0` 例: `@@ -1,0 +0,1 @@`) を reject し、後段の `current_new_line > 0` ガードが malformed hunk の最初の `+` 行を黙って取りこぼす false negative を阻止する）
- `src/engine/impact/mod.rs` - 影響分析オーケストレーター（CI 言語のみの diff は Pass1 前に skip、通常は 3パス方式: collect affected → stream refs → assemble）、`ParsedFile` キャッシュは `SourceBuf` を直接保持し mmap ゼロコピー経路を維持。affected 判定（`find_affected_symbols` / `symbol_overlaps_hunks`）は `range.end.line` を包含的に扱い、単一行シンボル（`start==end`）や複数行シンボルの最終行のみの変更も取りこぼさない（ゼロ幅 hunk = pure-delete は隣接行削除の過検出を避けるため半開区間を維持、pure-add の全体カバー判定は `hunk_end > sym_end`）。空 FileImpact（affected / callers / sig_changes すべて空）のスキップは streaming 経路と no-cross 分岐の両方に適用され、出力有無が同居ファイルの内容に依存しない。参照イベントは `RefVisitEvent` にファイル絶対列 `column`（AST point 照合 = test context 判定用）と trim 済み context 行内の相対列 `context_column`（import/re-export の statement 判定用）を両方持ち、座標系を混同しない。Pass2 の `RouteDecisionContext` は参照1件を `CallerRoute::{Normal, Low, Informational}` へ振り分ける判定と import facts cache だけを持ち、map/string pool の更新から分離する。informational は low より優先し、modified 以外の import と arity 変更は blocking 側へ残す。Rust の参照は `RefUsageRole`（CallCallee / FunctionValue / TypeConstrainedValue / Other）で AST 上の使われ方を分類し、**シグネチャのみ変更 (トップレベル arity 不変) されたシンボルへの FunctionValue 参照**（`register((a, my_system, b))` の**タプル/配列要素**・`f.after(..)` のメソッドレシーバ、Bevy `add_systems` パターン）は informational へ格下げする — トレイト境界に吸収されコンパイルが通るため。**直接引数 (`accept(my_system)`) は格下げしない** — 渡し先が `fn accept(_: fn(u32))` のような具体 fn ポインタ引数だと型変更で壊れるが AST だけでは渡し先シグネチャを解決できないため blocking 維持 (タプル/配列要素は fn ポインタを直接取る API がほぼ存在しないため安全に格下げできる)。ただし `let h: (fn(u32),) = (my_system,);` / `let h: [fn(u32); 1] = [my_system];` のように**タプル/配列の外側に明示型注釈が付く**場合は fn ポインタ型固定で壊れるため `tuple_or_array_has_explicit_type` (ネスト透過で let/const/static/cast の type 有無を確認) が blocking に戻す。`let x: fn(..) = f;` / `f as fn(..)` の fn ポインタ型固定 (TypeConstrainedValue) と callee 呼び出しも blocking 維持、removed は sig_changes に載らないため常に blocking。`cmd_impact` の人間向け `path:line` 表示は 1-indexed (エディタ互換)、構造化 JSON の `line` は従来どおり 0-indexed。lexer-only 言語のみの diff を skip する判定は `should_skip_ci_only_diff`（`diff_files_all_case_insensitive` + `ASTRO_SIGHT_FORCE_CI_LANG_IMPACT` の読み取り）に集約し、context と review が同じ入力に対して同じ判定をする。**Pass 2 の `all_file_contexts_case_insensitive` は入力集合が違う** (Pass 1 の存在/境界/parse フィルタ通過後の FileContext) ため多層防御として別判定のまま維持する
- `src/engine/impact/filters.rs` - cross-file 参照検索に含めるかの述語群。`should_include_for_cross_file` は `CrossFileFilterContext<'a>`（syms / hunks / sig_changes / diff_input / file_path / root / source / lang_id / changed_new_lines のファイル単位不変値）のメソッドで、コンテキストは affected ループの外で 1 度だけ構築する。フィルタ 3c は JS/TS の**名前空間オブジェクト export**（`export const ns = { fnA, fnB }` 形式のファサード）への「メンバー追加のみ」変更を除外する — 宣言ヘッダ行が未変更で、`is_js_ts_object_literal_variable` が variable_declarator の value を `object` と判定し、`diff::has_deletion_in_new_range` がシンボル range 内に `-` 行を見つけない場合のみ成立。メンバー追加は後方互換で既存メンバーだけを使う参照側は壊れないため、`ns` を import する全ファイルを影響先に列挙するのは過剰検出だった（Issue 2026-07-27-namespace-object-impact-granularity、実リポジトリで 5 ファイル 7 参照の FP）。メンバー削除・書き換えは `-` 行が range 内に出るため従来どおり blocking に残る。なお**プロパティ位置の識別子除外を共有ヘルパー `signature.rs::line_has_identifier` へ入れてはならない** — Ruby の特異メソッド定義 `def self.create(a)` は名前が `.` 直後に来るため、必須引数追加という破壊的変更が完全に無検出になる（回帰テスト `ruby_singleton_method_signature_change_is_detected` で固定）
- `src/commands/review/mod.rs` - `cmd_review` 本体（`cmd_dead_code` が `commands/dead_code.rs` にあるのと対称。`commands.rs` からは `pub use review::{CmdReviewOpts, cmd_review}` で再エクスポート）。3 種の早期 return は `emit_review_short_circuit`、dead-symbol フェーズは `review_dead_symbols` に分離。フェーズ計測は `commands/common.rs` の `timed` / `timed_ok`（**fallible 版はエラー時に `end` ログを出さない**＝`start` だけが残った phase が失敗箇所を指す既存挙動を保つ）
- `src/commands.rs` - CLI コマンド実装のハブ。引数 10 個超のコマンドは opts 構造体で受ける（`CmdAstOpts` / `CmdContextOpts` / `CmdImpactOpts` / `CmdReviewOpts`。`diff`/`diff_file` や `git`/`staged`/`hook` のような同型隣接引数を位置で渡すと取り違えてもコンパイルが通るため。本体側は先頭で `let &CmdXxxOpts { .. } = opts;` と一括分解して従来の局所変数名を維持し、本体書き換えによる取り違えの余地を作らない）
- `src/engine/impact/signature.rs` - diff の `+` / `-` 行から関数シグネチャ変更を検出（識別子境界で照合し、`foo` と `foo_bar` のような prefix 名を混同しない）。`detect_signature_changes` / `is_definition_header_in_changed_lines` / `is_symbol_in_changed_lines` はいずれも diff.rs の `HunkProgress` で hunk 行数を追跡し、本体の `+++ b/` 風コンテンツ行をヘッダ誤認しない。`is_signature_line` の Xojo 判定は 40 バイト窓を `eq_ignore_ascii_case` で走査して中間 `String` を確保しない（v26.6.110、`detect_signature_changes` で diff 全行 × affected 数の per-line アロケーションを排除）
- `src/engine/sequence.rs` - コールグラフから Mermaid シーケンス図を生成
- `src/engine/imports.rs` - ファイル間の import/export 関係を抽出（言語別 tree-sitter クエリ。Python は `aliased_import` (`import numpy as np`) / `relative_import` (`from . import x` / `from ..pkg import y`) も対象。PHP grouped use (`use App\Services\{Mailer, Logger};`) は宣言側 namespace prefix と合成して完全修飾で出力する）
- `src/engine/lint.rs` - YAML ルールによる AST パターンマッチ（tree-sitter クエリ + テキストパターン）
- `src/engine/cochange.rs` - git blame / log から共変更ファイルペアを検出。証拠の作り方は 2 段階で、第一選択は起点の**変更行**への `git blame`、証拠が `min_denominator` に届かない起点（base..HEAD に diff が無い＝`--paths` で任意ファイルを指定した場合 / 純粋追加 hunk のみ / blame が 1 コミットにしか遡れない）は `git log <base> -n 20 -- <file>` のファイル履歴へ fallback する（`CoChangeEvidence::{Blame,History}`、entry には history のときだけ `evidence` を付ける）。base 側に存在しない新規追加ファイルは履歴が無く起点にならない（追加コミット自身を証拠にすると同一コミットの全ファイルが confidence 1.0 になるだけで情報量が無い）。**閾値は raw `confidence` に適用し、平滑化した `score` はランキング専用**（score は `(co+α)/(denom+α+β)` の shrinkage 推定値で上限が `(d+α)/(d+α+β)`。既定 β=8 では分母 2 で上限 0.27 のため、閾値と突き合わせると変更行 blame 特有の小さい分母が構造的に出力不能になっていた）。score ゲートが要る場合だけ `--min-score`（既定 0.0 = 無効）。confidence の分母は「実際に集計対象となったコミット数」で、`max_files_per_commit` 超過（weight 0）のコミットは co と同様に分母からも外す。0 件の理由は `CoChangeDiagnostics`（起点数 / blame・history・証拠なしの内訳 / 各フィルタで落ちた候補数 / ソート済み `reasons` enum）で返し、「共変更なし」と「解析できなかった」を区別可能にする。`git diff-tree` の失敗は `commit_scan_failures`（0 なら JSON から省略）と `CoChangeDiagnosticReason::CommitScanFailed` で申告する＝旧実装は失敗を「変更ファイル 0 件」に畳む一方 `commits_analyzed` は逆引き集合サイズを返していたため、失敗コミットが「解析済み・共変更なし」と読める沈黙になっていた。`commits_analyzed` からは失敗分を引かない（この値は「証拠として集めたユニークコミット集合のサイズ」という別の量で、実際に集計対象となった件数は per-source の `entry.denominator` が持つ）。`analyze_cochange` 本体は薄いオーケストレーションで、`build_evidence_index`（証拠収集 → 診断反映 → SHA 逆引き）→ `aggregate_commits`（rayon fold/reduce）→ `assemble_entries`（閾値・ランキング）の 3 変換に分ける。100ファイル超のコミットはスキップ、`review --base` の missing_cochanges は同じ base を blame 解析にも渡す。**review の missing_cochanges だけ最小 support を 3 に上げる** (`review/missing_cochange.rs::REVIEW_COCHANGE_MIN_SAMPLES`、`review --cochange-min-samples` で上書き可、standalone `cochange` の既定 2 は不変)。変更行 blame は分母 2 の起点が普通に作られるため、「1 回だけ一緒に変わった」ペアが co=2/denom=2 = confidence 1.0 として最上位に並び、レビューのたびに同じ誤検出が出ていた (実測: 実リポジトリの missing_cochanges 6 件がすべて confidence 1.0 の FP)。support の要求を分子 (co_changes) の下限で表すのは、閾値を smoothed `score` 側へ移すと「分母が小さい起点は 100% 共変更でも構造的に出力不能」という以前の穴に戻るため。`confidence` は raw ratio のまま意味を変えない (Wilson 下限のような別指標を入れるなら追加フィールドにする)。「相方が直近 K コミットで更新済みなら抑制」は採らない — 直前に更新されたことは現在の変更に追随済みであることを意味せず (README を直した直後に API をもう一度変えるケース)、K を広げるほど偽陰性が増える。**`api.mod` / `api.rm` が空なら cochange を抑制する案も採らない** — cochange の目的は呼び出し規約**以外**の暗黙の結合 (レジストリと実装、schema と fixture、設定とドキュメント、source と生成物、feature flag と展開先) を拾うことで、公開 API 変更の有無で抑制するとそれらを構造的に落とす。「関数本体だけ変更したが過去 N 回常に実装ファイルも更新された」は cochange の意味では正しい警告で、review hook では既に informational (非 blocking)。代わりに**証拠を出す**: `MissingCochange` に `co_changes` (分子) と `denominator` (分母) を持たせ、hook では `n` / `d` として出力する (追加フィールドなので既存 consumer の JSON パースは壊れない)。`c` (confidence %) だけでは 1/1 と 5/5 がどちらも 100 になり、標本の大きさが読めないまま「100% 共変更」の表示だけでトリアージが判断してしまう。分母が算出できない場合は `d` を省略する (0 と読まれるのを避ける)
- `src/engine/snippet.rs` - コンテキストスニペット生成
- `src/models/` - Request/Response/AST ノード/Call/Reference/Impact/Sequence/Import/Lint/CoChange/DeadCode/Skip/Truncation 型定義（`truncation.rs` は「解析したが一部を対象外にした」部分的な打ち切りを表し、`skip.rs` の「コマンド全体を解析しなかった」大域 skip とは別物）
- `src/error.rs` - AstroError + ErrorCode（PathOutOfBounds 含む）
- `src/cache/store.rs` - BLAKE3 ベースのキャッシュ保存（~/.cache/astro-sight/、CLI の `ast` / `symbols` は呼び出し側 `cache_hash_for_path` で path + astro-sight バージョン込みハッシュを渡し、バージョン更新時に旧キャッシュを自動失効させる。put は temp + rename の atomic 書き込み、get は末尾 `}` 検証で truncated エントリを miss + 自己削除）
- `src/session/mod.rs` - NDJSON セッション処理（生行サイズで100MB上限、相対パスは `ASTRO_SIGHT_WORKSPACE` 基準、空文字・非 UTF-8 を含む `ASTRO_SIGHT_WORKSPACE` の不正値は fail-closed）
- `src/language.rs` - 言語検出（拡張子/shebang）
- `tools/usage-stats/src/main.rs` - Claude Code / Codex の利用ログ集計（`astro-sight` はシェル上の実行コマンドとして現れ、かつ既知サブコマンドを抽出できた場合のみ採用数に数える。パス文字列・プロンプト内の言及・`--version` / `--help` のようなサブコマンドなしの確認起動は除外。`--config <path>` や `/usr/bin/time -o <file>` 経由でも実サブコマンドを抽出。Codex は従来の `function_call` / `exec_command` と現行の `custom_tool_call` / `exec` 内 `tools.exec_command` の双方に対応し、JavaScript の文字列・コメント内は読み飛ばす。自動継続用の `wait` はコード分析や編集の選択ではないため採用率の分母から除外する。コマンド分割 (`split_top_level_segments`) はシェル引用符 `'...'` / `"..."` を尊重し、`codex exec "...&& astro-sight refs..."` のように引用符内へ埋め込まれた astro-sight コマンド例を実呼び出しと誤検出しない）
- `src/commands/tests/` - `commands` のユニットテスト（2026-08-06 に単一 15400 行ファイルから分割。`common.rs` が共有ヘルパー、`api_changes/` はテーマ別に rust_module_visibility / rust_reexport / rust_crate_kind / typescript / python / other_languages / compatibility_params / compatibility_react / removal / rename_move / general、ほかに input_validation / git_input / review_hook / dead_code / cochange）
- `tests/integration.rs` + `tests/integration/` - CLI 統合テスト（2026-08-06 に単一 12183 行ファイルから分割。ルートはインライン `mod integration` の宣言のみ＝テストターゲットのルートファイルでは `mod x;` が `tests/x.rs` を指すため、`tests/integration/` を module ディレクトリにするための包み。`support.rs` に `TestRepo` / `cargo_bin` 等の共有ヘルパーを集約）
- `tests/fixtures/` - 多言語テストフィクスチャ（sample.rb, sample.py, sample.go, sample.ts）

## Review Standards

- 修正対象は再現可能で根拠を示せる不具合に限る。推測や好みに基づく変更は行わない
- セキュリティ境界は fail-open にしない。設定値やサンドボックスが不正なら明示的に失敗させる
- **出力順は決定論的に保つ**。`std::collections::HashMap` / `HashSet` は RandomState のため反復順が実行ごとに変わる。ユーザーに見える配列を組み立てる際に `into_values()` / `iter()` の順をそのまま使わない — 入力 Vec の順を保つ (index をバケットに積む) か、明示的な全順序でソートする。ソート後に `truncate(N)` する箇所は特に危険で、同値キーだけで比較すると実行ごとに違う部分集合が落ちる (snapshot 比較とトリアージ結果の再現性が壊れる)
- **言語別の tree-sitter ノード名テーブルは実ノード名を `astro-sight ast` でダンプして照合する**。文法ごとにノード名が違うため、汎用スライスや他言語のテーブルを流用すると「1 つもマッチしない」形で静かに壊れる (エラーにならず `cx=1` のような無害に見える値を返す)。テーブルは `_` catch-all を置かず `LangId` を全列挙し、言語追加時にコンパイルエラーで気付けるようにする
- Grep / `grep` / `rg` を呼ぶ直前に検索パターン自体を確認し、関数・クラス・変数・型・定数・メソッド名を 1 つでも含む場合は `astro-sight refs --name/--names` を使う。ファイル種別や周辺タスクだけで判断しない
- コード変更の前には `astro-sight context --dir . --git`、変更後には `astro-sight impact --dir . --git` を実行する
- diff / PR 全体のレビュー依頼では、個別コマンドを積み上げる前に `astro-sight review --dir . --git` で全体像を確認する
- diff 全体の一括レビューでは `astro-sight review --dir . --git` も併用し、影響・共変更・API 差分・死蔵シンボルをまとめて確認する
- 公開 API や export を触った変更では `astro-sight dead-code --dir . --git` も併用して死蔵シンボルを確認する
- 構造確認が 2 手以上続くと分かっている場合（`symbols` → `imports` / `calls` / `sequence` 等）は最初から `session` にまとめ、プロセス起動コストと手順漏れを抑える
- 呼び出し順序が重要、または caller/callee の連鎖が 3 段以上になる場合は、`calls` の一覧だけで終えず `astro-sight sequence --path <file> --function <name>` で流れを可視化する
- `symbols` の 1 行情報だけで判断できない構文境界・parse エラー・巨大行の扱いは、Read/Grep ではなく `astro-sight ast --path <file> --line <n> --col <n>` で exact node を確認する
- 関連ファイルの変更漏れ確認は `review --dir . --git` の `missing_cochanges` または `cochange --dir . --paths <file>` を先に使い、ファイル名の勘だけで判断しない。0 件だったときは `diagnostics` / `cochange_diagnostics` を読み、「共変更が無い」のか「起点の証拠を作れなかった / 閾値で落ちた」のかを確認する
- 繰り返しの構造ルール確認には `astro-sight lint --path <file> --rules <rules.yaml>` を使い、アドホックなテキスト検索で代用しない
- 並列集約や大規模リポジトリ向けの変更では `/usr/bin/time -l` でピーク RSS を測定し、`par_iter().collect()` で不要な中間 Vec を全展開していないか確認する
- コードコメントは必要な箇所にだけ付け、付ける場合は日本語で簡潔に記述する

## Build & Test

```bash
make build    # or: cargo build
make test     # or: cargo test
make check    # clippy + fmt check
make help     # 全ターゲット表示
```

## Notes

- tree-sitter-kotlin 0.3.5 は旧 tree-sitter API のため、extern C ブリッジ（`src/language.rs` の `ffi_kotlin`）で対応
- tree-sitter 0.26 では Point/Range のフィールドがメソッドではなくパブリックフィールド
- QueryMatches は StreamingIterator（標準 Iterator ではない）

## Lexer-Only バックエンド (v26.6 で導入)

### 背景
tree-sitter で実用的に parse できない言語向けに、手書き lexer による fallback バックエンドを持つ。
case-insensitive な GLR バックトラッキング系の tree-sitter grammar は parse 中に 1GB/秒級でメモリが線形に膨張する事案 (Xojo 4 行 diff で 30GB OOMKilled の経緯あり) のため、tree-sitter-xojo は v26.6 で Cargo 依存ごと削除した。

### 言語モデルの分離
`src/language.rs` で型を 3 つに分けている:
- `TreeSitterLang` — `ts_language()` を持つのはこの型だけ。Rust/Python/JS 等 16 言語
- `LexerLang` — 手書き lexer で解析する言語 (現状 `Xojo`)
- `DetectedLang` — `TreeSitter(TreeSitterLang) | LexerOnly(LexerLang)` の判定結果型

`LangId::ts_language()` を呼ぶ前に `is_lexer_only()` で振り分ける義務がある (lexer-only に対する `ts_language()` は panic する)。

### コマンド別の挙動
lexer-only 言語に対する各コマンド:
- `symbols` / `refs` / `dead-code` — `src/engine/lexer/` 経由で動作 (定義ヘッダ列挙 + identifier 走査)。`dead-code` は Xojo の `#tag Event` ハンドラと `Inherits TestGroup` 派生クラスの引数なし `*Test` / `Setup` / `TearDown` を runtime entrypoint として除外する (`lexer/xojo.rs::runtime_entrypoint_lines`)
- `ast` / `calls` / `imports` / `lint` / `sequence` — `UNSUPPORTED_LANGUAGE` エラーを返す
- `context` / `impact` / `review` — 全 changed file が lexer-only のみの diff では cross-file 解析を skip する。`review` は `dead_symbols` も含めて空結果を返す (lexer 経路の cross-file refs は汎用名 noise が多いため将来 PR で本格対応)

### 強制無効化（デバッグ用）
context / impact / review / dead-code の skip 機構は次の env で解除できる (本番では設定しない):
- `ASTRO_SIGHT_FORCE_CI_LANG_IMPACT=1` — context / impact / review 全体 skip / impact pass2 で skip 解除
- `ASTRO_SIGHT_FORCE_CI_LANG_DEAD_CODE=1` — dead_code で skip 解除 (v26.6 では dead-code 自体は lexer 経路で動くため deprecate 扱い)

### 言語追加時の手順
tree-sitter で動かない言語を追加するには:
1. `language.rs` の `LexerLang` に variant を追加
2. `LangId` も対応する variant を追加し `detected()` を更新
3. `src/engine/lexer/mod.rs` に `LexerProfile` を新規追加 (keywords / コメント / 文字列区切り / 定義 keyword)
4. `profile_for()` の match arm に追加
